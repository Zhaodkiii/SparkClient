# 对话内「问报告」AI 解读需求文档（讨论稿）

> 范围说明：本文只讨论需求与产品/技术方案，不改动现有代码。目标是在对话过程中支持用户选择已保存的医疗记录/报告，并让 AI 基于报告内容快速给出合理解读。

## 1. 背景与目标

### Q：为什么要在对话里增加「问报告」？

A：用户已经在应用内保存了体检报告、检查报告、病历等医疗资料。当前用户如果想让 AI 解读报告，可能需要重新上传图片/PDF、复制内容，或者在病历页面与对话页面之间来回切换。新增「问报告」后，用户可以在当前对话里直接选择已保存报告，AI 自动带入报告结构化信息、附件、成员档案上下文，减少重复输入。

### Q：这个功能解决的核心问题是什么？

A：

1. 降低用户提问成本：不用再次上传报告。
2. 提升 AI 解读质量：比单纯图片/OCR 更适合传结构化字段、指标明细、报告结论。
3. 让成员档案和报告强绑定：AI 明确知道报告属于谁，避免多人家庭账号下误解读。
4. 保持对话连续性：用户可以围绕同一份报告连续追问。
5. 为后续“报告对比、异常指标趋势、就医建议、复查提醒”打基础。

### Q：一句话需求是什么？

A：在聊天输入区增加「问报告」入口，用户选择成员和报告后，报告作为一种特殊附件进入输入区预览，发送时将报告摘要、结构化指标、原始附件引用和成员上下文一并整理给 AI，并在消息流中以医疗报告卡片展示。

## 2. 入口设计

### Q：入口放在哪里？

A：建议有两个入口：

1. 输入区左侧固定悬浮按钮：在 `HanlinChatComposerView` / `ChatComposerContextTaskBar` 附近，作为高频入口，文案为「问报告」，图标建议 `doc.text.magnifyingglass` 或 `stethoscope`。
2. 成员档案绑定后显示入口：在 `ChatComposerRuntimeTogglesRow` 的“结合成员档案”状态下，当当前会话已绑定成员时，在成员相关区域显示「问报告」快捷入口。

### Q：为什么要两个入口？

A：左侧固定按钮负责“用户知道自己要问报告”的直接路径；成员档案绑定后的入口负责“当前上下文已经是某位成员”的自然路径。两者都打开同一个报告选择 Sheet，避免维护两套流程。

### Q：未绑定成员时点击「问报告」怎么办？

A：建议进入报告选择页时先展示成员选择区：

1. 如果有默认成员，自动选中默认成员并加载报告。
2. 如果没有默认成员，展示成员选择空状态，引导先选择成员。
3. 如果用户在报告选择页切换成员，应同步更新当前会话绑定成员。

### Q：当前会话已绑定成员时点击「问报告」怎么办？

A：报告选择页默认加载当前绑定成员的报告，并在顶部显示成员切换控件。用户切换成员后，当前会话的 `boundMemberID` 也要随之更新。

## 3. 报告类型与加载方式

### Q：报告类型有哪些？

A：从现有代码看，第一期建议优先支持两类“报告”：

1. 体检报告：`health-exam-reports`
   - 对应 DTO：`RemoteHealthExamReportWithAttachments`
   - 典型字段：机构、报告号、体检日期、体检类型、总结、附件、指标明细。
   - 现有加载接口：`listHealthExamReportsWithAttachments(memberID:)`

2. 医疗检查报告：`examination-reports`
   - 对应 DTO：`RemoteExaminationReportWithAttachments`
   - 典型字段：检验/检查项目、分类、子分类、检查时间、报告时间、机构、科室、医生、所见、结论、附件、指标明细。
   - 现有加载接口：`listExaminationReportsWithAttachments(memberID:)`

### Q：是否要支持 `MedicalReport` / 通用医疗报告？

A：需要确认。代码中存在 `MedicalReport` / `RemoteMedicalReport`，注释里提到“通用医疗报告（出院小结、病理等）”，但目前看到的列表页和首页入口主要是体检报告与检查报告。建议第一期先不混入，除非已有稳定列表 API、详情页和附件关系。

待确认问题：

1. “报告”在产品上是否只包含体检报告、检查报告？
2. 出院小结、病理报告、病历文书是否也要出现在「问报告」？
3. 处方、服药计划是否属于“问报告”，还是属于未来“问用药”？

### Q：如何加载报告？

A：建议报告选择页按当前成员加载：

1. 打开 Sheet。
2. 根据当前绑定成员或默认成员确定 `memberID`。
3. 并发请求：
   - `listHealthExamReportsWithAttachments(memberID:)`
   - `listExaminationReportsWithAttachments(memberID:)`
4. 合并成统一的 `ChatSelectableReport` 列表。
5. 按报告日期倒序排列。
6. 支持按类型筛选、关键词搜索、异常/有结论优先筛选。

### Q：指标明细如何加载？

A：现有列表页注释显示 `WithAttachments` 列表默认可能不含 `medExamDetails`，明细由列表页懒加载。因此「问报告」发送给 AI 前必须确认明细完整性：

1. 列表展示阶段可以只用摘要字段和附件。
2. 用户选中报告后，详情预览或发送前按需加载 `listMedExamDetails(memberID:businessType:businessID:)`。
3. 体检报告建议 `businessType = health_exam`，检查报告建议 `businessType = examination`，具体后端常量需确认。
4. 如果明细加载失败，仍允许发送摘要，但需要在 AI 入参中标记“明细缺失/加载失败”，避免 AI 误以为报告没有指标。

## 4. 报告选择页面

### Q：报告选择页应该是什么形态？

A：使用已有 Sheet 打开，内部建议为 `NavigationStack` 风格：

1. 顶部：标题「问报告」、关闭按钮。
2. 成员切换：头像/姓名/关系，支持切换成员。
3. 搜索框：搜索报告名、机构、报告号、结论、检查项目。
4. 类型筛选：全部、体检报告、检查/检验、影像、病理、异常。
5. 报告列表：卡片式展示。
6. 底部：已选报告数量 +「加入对话」按钮。

### Q：报告是否允许多选？

A：建议第一期支持多选，但默认引导单选：

1. 单份报告：适合“解读这份报告”。
2. 多份报告：适合“对比两次体检”“看指标变化”。
3. 限制建议：最多 3 份，避免 AI 上下文过长、卡片过多、用户难以理解。

如果当前发送链路不适合多报告，第一期也可以先单选，文档中预留多选模型。

### Q：报告列表卡片展示哪些信息？

A：

1. 类型标签：体检报告 / 检查报告 / 检验 / 影像 / 病理。
2. 标题：体检机构 + 日期，或检查项目名。
3. 时间：体检日期、报告日期或更新时间。
4. 机构/科室/医生。
5. 摘要：summary、findings、impression、detailText 等，最多 2-3 行。
6. 附件数量：图片/PDF 数量。
7. 异常提示：如果指标 flag、结论、summary 能识别异常，展示“含异常项”。
8. 所属病历：如果关联 `medicalRecord` / `medicalCase`，显示病历标题或就诊信息。

### Q：空状态如何设计？

A：

1. 当前成员没有报告：展示“暂无可解读报告”，提供“去上传报告”或“切换成员”。
2. 搜索无结果：展示“没有匹配的报告”，保留筛选和清空搜索。
3. 网络失败：展示重试按钮。
4. 未选择成员：展示成员列表入口。

## 5. 与成员档案绑定的关系

### Q：报告选择页切换成员后，为什么要更新当前会话绑定成员？

A：因为报告属于特定成员，AI 解读需要成员上下文。若用户从“张三”的对话里切到“李四”的报告，但会话仍绑定张三，会造成严重上下文污染。

### Q：如果对话已有绑定成员 A，用户选择成员 B 的报告怎么办？

A：建议弹出轻提示或二次确认：

“该报告属于 B，是否将当前对话切换为 B 的成员档案？”

默认操作建议：切换到 B。因为报告数据优先级高于旧绑定状态。

### Q：如果用户取消切换成员，还能选择 B 的报告吗？

A：建议不允许。报告附件必须和当前会话成员一致，除非未来支持“跨成员对比”这种明确场景。

## 6. 附件预览区与报告预览

### Q：选择报告后放在哪里？

A：放入 `HanlinChatInputView` 当前附件预览区，与图片/PDF 附件并列，但类型为“报告”。现有 `ChatComposerAttachmentKind` 只有 image/pdf/file，需求上建议增加一种逻辑类型：`report`。

### Q：是否需要和附件结合成公共预览页面？

A：建议需要，但分两层做：

1. 输入区缩略预览：统一附件 Strip，展示图片、PDF、文件、报告。
2. 点击后的详情预览：共用一个 `AttachmentPreview` 容器，根据类型渲染不同内容。

原因：报告不是普通文件，它包含结构化字段、指标明细、附件引用和成员信息。完全塞进现有文件预览会丢失医疗语义；完全另做一套又会让输入区交互割裂。

### Q：报告缩略卡如何设计？

A：建议尺寸和现有附件缩略图接近，但用医疗报告卡样式：

1. 左上角类型角标：体检 / 检查 / 影像 / 病理。
2. 主标题：报告标题或检查项目名。
3. 副标题：日期 + 机构。
4. 状态：已选 / 明细加载中 / 明细失败。
5. 关闭按钮：移除报告。
6. 多报告时横向排列。

### Q：报告详情预览如何设计？

A：点击报告附件后打开预览页，建议包含：

1. 顶部摘要：标题、成员、报告类型、日期、机构。
2. 关键结论：summary / findings / impression。
3. 异常指标：优先展示 flag 异常、超出参考范围、阳性、影像结论异常。
4. 全部指标：分组展示，可折叠。
5. 原始附件：图片/PDF 缩略图，可点击查看。
6. 发送给 AI 的范围说明：例如“将发送报告摘要、指标明细和附件引用，不发送无关成员资料”。

### Q：报告预览是否复用病历详细内时间线卡片？

A：可以复用视觉语言，但不建议直接复用 `MedicalCaseTimelineRow` 整体组件。

原因：

1. 时间线卡片强依赖病历详情页上下文：`medicalCaseID`、`workflowAPI`、`fileTransferService`、编辑入口、删除回调等。
2. 聊天消息卡片只需要展示和预览，不应带完整编辑能力。
3. 时间线是纵向事件流，聊天卡片是消息内附件，需要更紧凑。

建议抽取或仿照以下设计元素：

1. 左侧类型图标/颜色。
2. 标题 + 日期 + 状态 Badge。
3. `findings/impression/detailText` 三行摘要。
4. 附件 Pill 横向展示。
5. 分类颜色：实验室、影像、病理用现有 `ExaminationReportCategory` 颜色体系。

## 7. 发送给 AI 的数据整理

### Q：选择报告后，发送时 AI 应收到什么？

A：建议不要只发“报告 ID”，也不要直接塞完整 OCR。应构造一个结构化上下文包：

```json
{
  "type": "medical_report_context",
  "member": {
    "id": 123,
    "name": "成员昵称",
    "gender": "如有",
    "age": "如有"
  },
  "reports": [
    {
      "source_type": "health_exam_report | examination_report",
      "id": 1,
      "title": "报告标题",
      "date": "2026-05-21",
      "organization": "机构",
      "department": "科室",
      "doctor": "医生",
      "summary": "摘要",
      "findings": "所见",
      "impression": "结论",
      "abnormal_items": [],
      "detail_items": [],
      "attachments": []
    }
  ],
  "user_question": "用户输入的问题",
  "safety_instruction": "仅做健康科普和就医建议，不替代医生诊断"
}
```

### Q：如何控制上下文长度？

A：

1. 优先发送结构化字段和异常指标。
2. 指标过多时：全部指标本地保留，AI 入参只发异常项 + 关键正常项 + 总数统计。
3. 原始 OCR 不直接全量发送，除非报告结构化字段为空。
4. 附件只发送已上传文件引用、缩略摘要或 OCR 摘要，不重复上传已有文件。
5. 多报告对比时，按时间排序并突出同名指标变化。

### Q：AI 回答应该遵守哪些安全边界？

A：

1. 不做诊断定论。
2. 对危急值、明显异常、红旗症状提示尽快就医。
3. 明确“需结合症状、病史、医生面诊”。
4. 不建议用户自行停药、换药、延误治疗。
5. 对儿童、孕产妇、老人、慢病患者加强提示。
6. 当报告缺失参考范围或单位时，不强行判断高低。

### Q：用户没有输入文字，只选择报告直接发送怎么办？

A：默认问题可以是：

“请帮我解读这份报告，重点说明异常项、可能含义、需要关注的问题和建议下一步怎么做。”

发送前输入框也可以自动填充轻提示，但不强制插入文本。

## 8. 消息流与会话卡片 UI

### Q：用户消息里怎么展示已发送报告？

A：用户消息气泡内展示报告附件卡：

1. 标题：报告名。
2. 类型：体检报告 / 检查报告。
3. 成员：成员昵称。
4. 日期与机构。
5. 摘要 1-2 行。
6. 附件数量或指标数量。
7. 点击进入报告预览。

### Q：AI 回复里是否需要展示引用的报告？

A：建议需要。AI 回复顶部或结尾可以有“基于以下报告解读”的引用卡片，便于用户知道 AI 依据来源。

### Q：会话卡片能否复用病历详细内时间线卡片？

A：建议不要直接复用完整时间线 Row，而是抽取一个轻量 `MedicalReportSummaryCard` 的设计规范：

1. 可用于聊天附件预览。
2. 可用于消息流中的报告卡片。
3. 可用于报告选择页列表。
4. 可复用时间线卡片里的颜色、Badge、附件 Pill，但不要带编辑/删除/导航到病历编辑的行为。

### Q：时间线卡片设计需要注意哪些点？

A：

1. 避免聊天卡片过高：消息流内最多展示三行摘要。
2. 不显示病历详情页专用操作：编辑、删除、跳转复杂详情页。
3. 点击行为要单一：打开报告预览，而不是进入病历详情编辑。
4. 多附件报告要显示数量，不要在聊天消息内展开所有图片。
5. 异常项要突出，但避免制造焦虑，文案用“需关注”而不是“危险”。
6. 对同一报告重复发送时，要能识别报告 ID，避免消息里出现无法区分的重复卡。
7. 深色模式、动态字体、长机构名、长检查名都要可读。

## 9. 关键状态与边界场景

### Q：报告被删除或成员切换后，草稿中的报告怎么办？

A：

1. 如果报告已在草稿里但服务器删除：发送前校验失败，提示“报告不存在或已删除”。
2. 如果切换成员：清空不属于新成员的已选报告，或二次确认。
3. 如果用户退出会话再回来：是否保留草稿报告需跟现有附件草稿策略一致。

### Q：报告附件是否需要重新上传？

A：已保存报告的附件理论上已有 `ManagedFile`/OSS 引用，不应该重新上传。发送给 AI 时应传附件引用、文件 ID、业务类型和业务 ID。只有本地新选文件才走现有附件上传/OCR。

### Q：报告选择后是否可以继续添加普通附件？

A：可以。报告附件和普通附件可以并存，但发送时需要区分：

1. 普通附件：用户临时上传的新材料。
2. 报告附件：系统内已有医疗记录。
3. 如果两者都存在，AI prompt 需要明确“已保存报告”和“补充附件”的关系。

### Q：报告数量过多怎么办？

A：

1. 默认按时间倒序。
2. 提供搜索和类型筛选。
3. 可增加“最近 3 个月 / 最近 1 年”筛选。
4. 优先展示含异常项、含附件、含结论的报告。

## 10. 埋点与可观测性

### Q：需要记录哪些事件？

A：

1. 点击「问报告」入口：来源是固定按钮还是成员档案入口。
2. 报告选择页打开/关闭。
3. 成员切换。
4. 报告类型筛选。
5. 报告选中/取消。
6. 报告加入输入区。
7. 报告发送成功/失败。
8. 明细加载成功/失败。
9. AI 解读生成成功/失败。

### Q：日志里不能记录什么？

A：不能记录完整报告内容、OCR 原文、身份证号、电话号码等敏感信息。日志只记录 reportID、类型、数量、耗时、错误码。

## 11. 分期建议

### 第一期：最小可用

1. 输入区增加「问报告」入口。
2. Sheet 选择当前成员的体检报告和检查报告。
3. 支持切换成员并同步会话绑定。
4. 单选报告加入附件预览区。
5. 发送时整理结构化摘要给 AI。
6. 用户消息展示报告卡片。

### 第二期：增强体验

1. 多选报告。
2. 指标明细懒加载与异常项提取。
3. 报告详情预览页。
4. AI 回复引用报告卡。
5. 报告对比能力。

### 第三期：深度医疗工作流

1. 异常指标趋势。
2. 复查提醒。
3. 和病历、处方、服药计划联动。
4. 基于报告生成结构化问诊问题。
5. 医生就诊准备清单。

## 12. 需要继续确认的问题清单

1. 「问报告」第一期是否只支持体检报告和检查报告？
2. 是否允许多选报告？如果允许，最多几份？
3. 报告切换成员时，是否直接更新当前会话绑定成员，还是必须二次确认？
4. 选择报告后，输入框是否自动填充默认问题？
5. 报告预览页是否需要展示完整指标，还是只展示异常项？
6. AI 是否需要读取原始附件图片/PDF，还是只读取结构化字段？
7. `businessType` 的后端常量是否确定为 `health_exam` / `examination`？
8. 通用 `MedicalReport` 是否已有列表查询和附件关系？是否纳入第一期？
9. 聊天消息内的报告卡点击后，是打开只读预览，还是跳转到原报告详情页？
10. 普通附件和报告附件同时存在时，AI 应如何描述二者关系？
11. 已保存报告的 OCR 原文是否允许发送给 AI？是否需要用户授权？
12. 报告内容进入 AI 上下文是否需要单独隐私提示？
13. AI 解读结果是否需要支持“一键保存为报告解读记录”？
14. 是否要在会话列表缩略图中展示报告附件？
15. 报告被删除后，历史聊天消息里的报告卡如何展示？

## 13. 建议的验收标准

1. 未绑定成员时，点击「问报告」可以选择成员并加载该成员报告。
2. 已绑定成员时，点击「问报告」默认加载当前成员报告。
3. 在报告选择页切换成员后，当前会话绑定成员同步变化。
4. 选择报告后，输入区出现报告类型附件预览。
5. 报告附件可以移除、点击预览、随消息发送。
6. 发送给 AI 的上下文包含报告类型、报告 ID、成员 ID、摘要、结论、关键指标和附件引用。
7. AI 能在无用户文字输入时默认按“解读报告”回答。
8. 消息流能展示报告卡片，并能区分普通附件和报告附件。
9. 报告明细加载失败时有降级提示，不阻断基础解读。
10. 日志和埋点不泄露完整医疗隐私内容。

