# 对话常规变更需求文档

> 范围说明：本文按工单管理对话模块的常规体验与工程变更需求。工单号从 `000001` 递增，新增需求追加在文档底部；已有模块如已实现，仅补充工单号、状态、涉及文件和验收口径。

## 工单索引

| 工单号 | 工单名 | 状态 | 范围 |
| --- | --- | --- | --- |
| `CHAT-000001` | 发送前检查聊天场景模型并统一打开 API Keys 设置 | 已实现/补文档 | 会话内发送前模型可用性校验、无模型 Alert、统一 Sheet 打开设置页 |
| `CHAT-000002` | 会话列表支持图标编辑与置顶排序 | 已实现 | `ChatThread` 图标字段、客户端/服务端/存储/序列化、列表编辑、置顶、排序 |

## 工单 `CHAT-000001`：发送前检查聊天场景模型并统一打开 API Keys 设置

### 工单状态

已实现能力补充需求归档。后续如需要改文案或统一 Sheet 接入方式，可在本工单下追加变更记录。

## 1. 背景

### Q：为什么需要在发送对话前检查聊天模型？

A：当前会话页输入区可以直接触发发送，但如果聊天场景没有可用模型，用户会在发送链路后段才遇到失败。这个反馈时机太晚，也不利于引导用户去配置模型密钥或启用模型。

现网在会话列表页新建对话时已经有类似保护：

- `ChatConversationListPage.swift:72-79`：无可用模型时弹出 `chat.list.no_available_model.*` 提示。
- `ChatConversationListPage.swift:80-91`：点击“前往设置”后用 Sheet 打开 `APIKeysSettingsView`。

会话详情页也应复用同一套文案与设置入口，在用户点击发送前先做轻量校验。

### Q：这次常规变更解决什么问题？

A：

1. 发送前即可发现“聊天场景没有可用模型”的问题。
2. 与新建会话入口保持一致的用户提示。
3. 避免进入 `SendChatMessageUseCase` 后才失败，减少无效发送状态、流式状态和错误气泡。
4. 会话内所有 Sheet 入口统一收敛到 `ToolInteractionPresentationSheet`，避免 `ChatView` 持续增加多个 `.sheet` 状态。

## 2. 目标

### Q：一句话需求是什么？

A：用户在会话内点击发送时，先检查聊天场景模型列表是否为空；如果没有可用聊天模型，则不发送消息，弹出“无可用对话模型”提示；用户点击“前往设置”后，通过会话内统一的 `ToolInteractionPresentationSheet` 打开 `APIKeysSettingsView`。

### Q：判定“没有可用聊天模型”的数据来源是什么？

A：以现有聊天场景模型列表为准：

- `AIConfigModels.swift:532-533`：`AIScenarioRemoteBundlesCollection.chat` 是聊天场景模型集合。
- `ChatDetailViewModel.swift:307-317`：`refreshChatModelPicker(for:)` 从 `effectiveScenarioBundles().chat.models` 更新 `chatScenarioModels`。
- `ChatView.swift:107`：`HanlinChatComposerView` 当前已接收 `detailViewModel.chatScenarioModels`。

需求判定建议：`detailViewModel.chatScenarioModels.isEmpty == true` 时视为当前会话没有可用聊天模型。

## 3. 交互流程

### Q：用户点击发送时的流程是什么？

A：

1. 用户点击输入区发送按钮。
2. `ChatView` 的 composer `onSend` 回调被触发。
3. 先收起键盘。
4. 检查当前聊天场景模型列表是否为空。
5. 如果不为空：继续调用 `detailViewModel.startSendingCurrentDraft()`。
6. 如果为空：不进入发送链路，弹出“无可用对话模型”提示。
7. 用户点击“前往设置”：打开 API Keys 设置页。
8. 用户点击“取消”：关闭弹窗，不发送消息。

流程示意：

```text
点击发送
  ↓
KeyboardDismissHelper.dismissKeyboard()
  ↓
chatScenarioModels 是否为空？
  ├─ 否 → detailViewModel.startSendingCurrentDraft()
  └─ 是 → showNoAvailableChatModelAlert = true
              ↓
          [前往设置] → ToolInteractionPresentationSheet → APIKeysSettingsView
          [取消]     → 关闭 Alert
```

### Q：哪些 Composer 样式需要检查？

A：都需要。

- `ChatView.swift:75-99`：`.signal` 样式的 `ChatComposerView(onSend:)`。
- `ChatView.swift:100-145`：`.hanlin` 样式的 `HanlinChatComposerView(onSend:)`。

两个入口都应走同一个发送前检查方法，避免双分支逻辑漂移。

建议抽出 `private func sendCurrentDraftIfChatModelAvailable()`，两个 `onSend` 均调用它。

## 4. 弹窗需求

### Q：弹窗文案是什么？

A：复用会话列表页现有本地化文案：

```swift
.alert(L10n.text("chat.list.no_available_model.title"), isPresented: $showNoAvailableChatModelAlert) {
    Button(L10n.text("chat.list.no_available_model.action")) {
        // 打开 API Keys 设置
    }
    Button(L10n.text("common.cancel"), role: .cancel) {}
} message: {
    Text(L10n.text("chat.list.no_available_model.message"))
}
```

当前已有文案：

| Key | 中文含义 |
| --- | --- |
| `chat.list.no_available_model.title` | 无法新建/发送对话 |
| `chat.list.no_available_model.message` | 当前没有可用的对话模型，请先配置模型密钥或启用可用于对话的模型 |
| `chat.list.no_available_model.action` | 前往设置 |
| `common.cancel` | 取消 |

### Q：标题是否要继续使用“无法新建对话”？

A：短期可复用现有 key，保证列表页和会话页体验一致；但从语义上看，会话内更准确的标题应是“无法发送消息”。如果后续希望更精确，可新增：

- `chat.detail.no_available_model.title`
- `chat.detail.no_available_model.message`
- `chat.detail.no_available_model.action`

第一期建议先复用现有 key，减少本地化改动面。

## 5. API Keys 设置页打开方式

### Q：为什么会话内不直接新增 `.sheet(isPresented:)`？

A：`ChatView` 当前已经有统一的工具/对话交互 Sheet：

- `ChatView.swift:354-391`：通过 `detailViewModel.toolInteractionCoordinator.activePresentation` 驱动 `.sheet(item:)`。
- `ChatToolInteractionSheets.swift:641-720`：`ToolInteractionPresentationSheet` 根据 `ToolInteractionSnapshot` 分发具体页面。
- `ToolInteractionCoordinator.swift:4-7`：协调器目标是串行处理同一时间只能出现一个交互窗口。

如果在 `ChatView` 里再新增一个 `showAPIKeysSettingsSheet`，会带来多个 Sheet 状态并存的问题：工具授权、问报告、系统消息设置、API 设置可能互相抢展示。会话内应统一进入 `ToolInteractionPresentationSheet`。

### Q：需要怎么接入统一 Sheet？

A：建议新增一个非阻塞展示类型：

```swift
enum ToolInteractionSnapshot {
    case apiKeysSettings
}
```

并在 `ToolInteractionCoordinator` 增加：

```swift
func presentAPIKeysSettings()
```

`apiKeysSettings` 与 `toolPreview`、`systemMessageSettings`、`askReportPicker` 一样属于“只展示，不阻塞工具执行 continuation”的 Sheet 类型。

`requiresForcedSheetDismiss` 建议返回 `false`，允许用户下滑关闭。

### Q：`ToolInteractionPresentationSheet` 里怎么展示？

A：新增 case：

```swift
case .apiKeysSettings:
    CompatibleNavigationContainer(legacyStackStyle: true) {
        APIKeysSettingsView(viewModel: aiSettingsViewModel)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button(L10n.text("common.done")) {
                        coordinator.dismissAPIKeysSettings(id: active.id)
                    }
                }
            }
    }
```

注意：`ToolInteractionPresentationSheet` 当前没有 `AISettingsViewModel` 依赖，需要从 `ChatView` 或 `ChatDetailViewModel` 挂入。不要在 Sheet 内临时 new 一个设置 ViewModel，避免状态、加载时机和依赖来源不一致。

## 6. 状态设计

### Q：`ChatView` 需要新增哪些状态？

A：建议只新增 Alert 状态：

```swift
@State private var showNoAvailableChatModelAlert = false
```

不建议新增：

```swift
@State private var showAPIKeysSettingsSheet = false
```

原因：会话内设置页打开应走 `ToolInteractionCoordinator`，而不是 `ChatView` 本地 `.sheet`。

### Q：发送前检查方法放在哪里？

A：建议放在 `ChatView` 内，原因是：

1. `ChatView` 同时拥有 composer 回调和 `detailViewModel.chatScenarioModels`。
2. 这个检查是 UI 交互门禁，不属于真正发送业务。
3. 可以同时覆盖 `.signal` 和 `.hanlin` 两种 composer。

建议方法：

```swift
private func sendCurrentDraftIfChatModelAvailable() {
    KeyboardDismissHelper.dismissKeyboard()
    guard detailViewModel.chatScenarioModels.isEmpty == false else {
        showNoAvailableChatModelAlert = true
        return
    }
    detailViewModel.startSendingCurrentDraft()
}
```

两个 `onSend` 从：

```swift
KeyboardDismissHelper.dismissKeyboard()
detailViewModel.startSendingCurrentDraft()
```

替换为：

```swift
sendCurrentDraftIfChatModelAvailable()
```

## 7. 涉及文件与改动内容

| 文件 | 改动内容 | 影响 |
| --- | --- | --- |
| `SparkClient/Projects/Features/Chat/Presentation/ChatView.swift` | 新增 `showNoAvailableChatModelAlert`；新增统一发送前检查方法；`.signal` 与 `.hanlin` 的 `onSend` 均改走检查方法；新增无模型 Alert；Alert 的“前往设置”调用 coordinator 打开设置页 | 会话内发送入口增加前置保护 |
| `SparkClient/Projects/Core/AIRuntime/ToolInteractionSnapshot.swift` | 新增 `apiKeysSettings` 展示类型；`requiresForcedSheetDismiss` 返回 `false` | 统一 Sheet 路由增加设置页类型 |
| `SparkClient/Projects/Core/AIRuntime/ToolInteractionCoordinator.swift` | 新增 `presentAPIKeysSettings()`、`dismissAPIKeysSettings(id:)`、对应 `PendingOutcome` 和默认 dismiss outcome | 设置页进入统一队列，避免多 Sheet 冲突 |
| `SparkClient/Projects/Features/Chat/Presentation/ChatToolInteractionSheets.swift` | `ToolInteractionPresentationSheet` 新增 `apiKeysSettings` case；接收 `AISettingsViewModel`；展示 `APIKeysSettingsView` | 会话内可通过统一 Sheet 打开 API Keys 设置 |
| `SparkClient/Projects/Features/AISettings/Presentation/Providers/APIKeysSettingsView.swift` | 无需修改，复用现有设置页 | 复用现有 Provider/API Key 配置能力 |
| `SparkClient/Projects/App/Resources/*/Localizable.strings` | 第一阶段可不改，复用 `chat.list.no_available_model.*`；若产品要求语义更精确，再新增 detail 维度文案 | 控制本地化改动范围 |

## 8. 实现方式建议

### 8.1 ChatView 发送入口收敛

`ChatView.swift:75-146` 当前两个 composer 分支都内联调用发送。建议改为统一方法，避免后续一个入口漏检查。

关键点：

1. 先收键盘。
2. 再判断 `chatScenarioModels`。
3. 为空时只弹 Alert，不清空草稿、不上传附件、不触发发送状态。
4. 不为空时保持现有发送逻辑。

### 8.2 Alert 打开设置页

Alert 的“前往设置”按钮不直接控制本地 sheet，而是调用：

```swift
detailViewModel.toolInteractionCoordinator.presentAPIKeysSettings()
```

如果后续希望隐藏 coordinator 细节，也可以在 `ChatDetailViewModel` 增加薄封装：

```swift
func presentAPIKeysSettings() {
    toolInteractionCoordinator.presentAPIKeysSettings()
}
```

### 8.3 ToolInteractionPresentationSheet 传入 AISettingsViewModel

现网列表页 `ChatConversationListPage.swift:80-91` 直接使用 `aiSettingsViewModel` 创建 `APIKeysSettingsView`。

会话内若复用同一个页面，也需要能拿到同一个设置 ViewModel。推荐从上层注入：

```swift
ToolInteractionPresentationSheet(
    active: active,
    coordinator: detailViewModel.toolInteractionCoordinator,
    ...
    aiSettingsViewModel: aiSettingsViewModel
)
```

如果 `ChatView` 当前没有该依赖，需要从组装层补齐，而不是在子 View 内创建新实例。

## 9. 验收标准

### 功能验收

1. 当 `chatScenarioModels` 不为空时，点击发送行为与现网一致。
2. 当 `chatScenarioModels` 为空时，点击发送不进入 `startSendingCurrentDraft()`。
3. 无模型时弹出 `chat.list.no_available_model.*` 对应提示。
4. 点击“取消”关闭弹窗，草稿、附件、问报告预览保持不变。
5. 点击“前往设置”后，通过 `ToolInteractionPresentationSheet` 打开 `APIKeysSettingsView`。
6. API Keys 设置页点击“完成”后关闭 Sheet。
7. 设置页关闭后，原会话页面状态保持，不丢失输入内容。
8. `.signal` 与 `.hanlin` 两种 composer 都能触发同样检查。

### 状态验收

1. 无模型 Alert 展示期间，不创建用户消息。
2. 不启动附件上传。
3. 不启动 OCR。
4. 不启动 AI 流式请求。
5. 不新增空 assistant 占位消息。
6. 不影响工具授权、问报告、系统消息设置等已有 `ToolInteractionPresentationSheet` 队列展示。

### 回归验收

1. 会话列表页原有“无可用模型 → 前往设置”流程不受影响。
2. 已有 `ToolInteractionPresentationSheet` case：授权、问题、成员选择、工具预览、系统消息设置、问报告选择均可正常打开和关闭。
3. 配置模型后返回会话，重新刷新模型列表后可正常发送。

## 10. 风险与注意事项

### Q：只检查 `chatScenarioModels.isEmpty` 是否足够？

A：第一阶段足够，因为 `refreshChatModelPicker(for:)` 的数据源就是 `effectiveScenarioBundles().chat.models`。但需要注意：如果模型列表不为空，但实际 API Key 不可用，发送仍可能失败。这个属于运行时调用失败，不应在本需求内扩大处理范围。

### Q：如果进入会话时模型列表还没刷新完成怎么办？

A：现网 `ChatView.swift:343-349` 已在 `.task(id: threadID)` 中刷新模型选择器。发送前检查使用当前 `chatScenarioModels`。如果担心刚进入页面时列表尚未完成加载，可以在发送前补一次轻量刷新：

```swift
if detailViewModel.chatScenarioModels.isEmpty {
    _ = await detailViewModel.refreshChatModelPicker(for: threadID)
}
```

但这会把发送方法变成 async，第一期建议先根据现有刷新结果判断，除非产品明确要求“刚进入页面立即点发送也必须等待刷新完成”。

### Q：为什么不把检查放到 `SendChatMessageUseCase`？

A：`SendChatMessageUseCase` 仍然应该保留后端式防御，避免无模型时真正发送。但用户体验层面的 Alert 和设置页跳转属于 UI 门禁，放在 `ChatView` 更合适。  

最终建议是双层保护：

1. UI 层：提前提示并引导设置。
2. UseCase 层：如果仍无模型，抛出明确错误，避免异常状态。

## 11. 后续可扩展项（CHAT-000001）

1. 新增会话详情专用本地化 key，把“无法新建对话”调整为“无法发送消息”。
2. 设置页关闭后自动刷新 `chatScenarioModels`，若已有可用模型，可自动选中默认模型。
3. 在发送按钮 disabled/loading 状态中体现“模型配置不可用”，减少用户点击后才弹窗。
4. 支持从 Alert 直接打开“模型配置”指定 tab，而不是 API Keys 页根页面。
5. 在调试日志中记录无模型拦截事件，但不记录用户输入内容。

## 工单 `CHAT-000002`：会话列表支持图标编辑与置顶排序

### 工单状态

新需求，待实现。

### 1. 背景

### Q：为什么会话需要图标、编辑和置顶？

A：当前会话列表 `ChatConversationListPage` 只展示标题、时间、最近消息预览和可选图片缩略图，列表行的长按菜单与滑动操作也只支持删除。随着会话数量增加，用户需要更快地区分常用会话、重要会话和不同用途的会话。

参考 `hanlin-ai` 旧实现：

1. 列表按 `isPinned` 分组，置顶会话显示在未置顶会话上方。
2. 会话卡片展示 icon / title / 时间 / 预览。
3. 长按菜单支持编辑图标、置顶/取消置顶、删除。
4. 左滑/右滑支持快捷置顶、编辑、删除。
5. 编辑 Sheet 支持修改标题、图标、颜色。

### Q：本工单目标是什么？

A：在 SparkClient 当前会话列表中补齐会话个性化和常用会话管理能力：

1. `ChatThread` 增加会话图标相关字段。
2. 会话列表支持置顶/取消置顶。
3. 会话列表支持编辑标题、图标、颜色。
4. 列表排序支持置顶优先。
5. 客户端、服务端、CoreData、本地序列化、同步链路都能保存并恢复这些字段。

## 2. 现网位置

### Q：当前 SparkClient 相关代码在哪里？

A：

| 模块 | 文件 | 当前情况 |
| --- | --- | --- |
| 会话领域模型 | `SparkClient/Projects/Core/Domain/Entities/ChatThread.swift` | `ChatThread` 只有 `title`、模型配置、成员绑定、时间等字段，没有 icon/color/pin 字段 |
| 会话列表行 | `SparkClient/Projects/Features/Chat/Presentation/ChatConversationListPage.swift:116-190` | `threadRow(_:)` 展示标题、时间、预览、图片缩略图；长按/右滑只支持删除 |
| 列表 ViewModel | `SparkClient/Projects/Features/Chat/Presentation/ChatListViewModel.swift` | 支持加载、搜索、创建、删除；没有编辑标题/图标/置顶能力 |
| 存储协议 | `SparkClient/Projects/Features/Chat/Domain/ChatStoreProtocols.swift` | `ChatThreadStoring` 没有更新 metadata / pin 的接口 |
| CoreData 存储 | `SparkClient/Projects/Features/Chat/Infrastructure/CoreDataChatStore.swift` | `loadThreadListItems()` 当前按 `latestMessageAt` 倒序；`ChatThreadEntity` 未见 icon/pin 字段 |
| 列表投影 | `ChatThreadListItem` | 只包含 `thread`、预览、时间、未读、图片缩略图 |

## 3. 数据模型需求

### Q：`ChatThread` 需要增加哪些字段？

A：建议最小可用字段如下：

```swift
struct ChatThread: Identifiable, Codable, Equatable, Sendable {
    let iconName: String?
    let iconColorName: String?
    let isPinned: Bool
    let pinnedAt: Date?
}
```

字段说明：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `iconName` | `String?` | SF Symbol 名称，例如 `bubble.left.circle`、`heart.text.square` |
| `iconColorName` | `String?` | 客户端约定色值 key，例如 `blue`、`green`、`pink`；避免直接存 SwiftUI `Color` |
| `isPinned` | `Bool` | 是否置顶 |
| `pinnedAt` | `Date?` | 置顶时间，用于多个置顶会话内部排序 |

### Q：为什么需要 `pinnedAt`，不能只用 `isPinned`？

A：只用 `isPinned` 可以完成置顶分组，但无法表达多个置顶会话的内部顺序。`pinnedAt` 可以让最近置顶的会话排在置顶区上方，也方便后续做“取消置顶后恢复普通排序”。

### Q：`Codable` 如何兼容旧数据？

A：需要注意 `Bool` 和 `Date` 新字段的解码兼容：

1. 如果保持自动合成 `Codable`，新增非可选 `isPinned: Bool` 会导致旧 JSON 缺字段时解码失败。
2. 建议为 `ChatThread` 增加自定义 `init(from:)`，对旧数据默认：
   - `iconName = nil`
   - `iconColorName = nil`
   - `isPinned = false`
   - `pinnedAt = nil`
3. 或者把新增字段都设计为可选，再提供计算属性：
   - `var isPinnedForDisplay: Bool { isPinnedRaw ?? false }`

第一期建议使用自定义解码，让领域模型对外保持清晰的 `Bool`。

## 4. 客户端存储与同步

### Q：CoreData 需要怎么改？

A：`ChatThreadEntity` 需要新增字段：

| CoreData 字段 | 类型 | 默认值 |
| --- | --- | --- |
| `iconName` | String, Optional | nil |
| `iconColorName` | String, Optional | nil |
| `isPinned` | Boolean | false |
| `pinnedAt` | Date, Optional | nil |

需要同步更新：

1. `CoreDataChatStore.toThread(_:)`：从 entity 读出新增字段。
2. `createThread(...)`：新建会话写入默认值。
3. `upsertRemoteThreads(_:)`：远端线程落库时写入新增字段。
4. `makeThreadListProjectionItem(...)`：列表投影读取新的 `thread` 字段即可。
5. 当前项目按全新项目处理，不需要考虑旧版本 CoreData 迁移；只需要保证新建库 schema 完整、默认值正确。

### Q：服务端接口需要怎么考虑？

A：需要服务端线程模型同步增加字段。当前服务端模型位置：

`/Users/hua/Downloads/Reference/SparkService/chat_sync/models.py:7-53`

现有 `ChatThread` 字段包含 `title`、`scenario`、模型参数、`image_delivery_mode`、删除状态和时间字段，但没有会话图标、颜色、置顶状态。需要在该模型中增加与客户端可映射的字段：

```json
{
  "icon_name": "bubble.left.circle",
  "icon_color_name": "blue",
  "is_pinned": true,
  "pinned_at": "2026-05-28T12:00:00Z"
}
```

建议 Django 字段：

```python
icon_name = models.CharField(max_length=128, blank=True, default="")
icon_color_name = models.CharField(max_length=64, blank=True, default="")
is_pinned = models.BooleanField(default=False, db_index=True)
pinned_at = models.DateTimeField(null=True, blank=True, db_index=True)
```

需要确认：

1. 创建会话接口是否接收这些字段。
2. 更新会话接口是否支持局部更新 metadata。
3. 线程列表接口是否返回这些字段。
4. 多端同步时，置顶状态以服务端为准还是本地优先。
5. `updatedAt/serverUpdatedAt` 是否因置顶或编辑图标而更新。
6. 服务端 `Meta.ordering` 当前为 `["-updated_at"]`，如果服务端列表也负责排序，需要调整为置顶优先；如果客户端负责排序，服务端至少要完整返回 `is_pinned/pinned_at`。

第一期建议：编辑标题/图标/颜色/置顶都视为会话 metadata 更新，需要进入 outbox 或同步队列，避免多端状态不一致。

## 5. 列表排序需求

### Q：会话列表如何排序？

A：参考 `hanlin-ai` 的 `filteredChatRecords`：

1. 先分组：`isPinned == true` 的会话在上，未置顶在下。
2. 置顶组内部排序：
   - 优先按 `pinnedAt` 倒序；
   - 如果 `pinnedAt` 为空，按 `latestMessageAt` 倒序兜底。
3. 未置顶组内部按 `latestMessageAt` 倒序。
4. 搜索结果也要保持同样排序，而不是搜索后丢失置顶分组。

当前 `CoreDataChatStore.loadThreadListItems()` 在 `CoreDataChatStore.swift:115` 只按 `latestMessageAt` 倒序，需要调整为置顶优先排序。

建议排序函数集中在一个地方，例如：

```swift
private static func sortThreadListItems(_ items: [ChatThreadListItem]) -> [ChatThreadListItem]
```

`loadThreadListItems()`、`ChatListViewModel.search(text:)` 都复用同一排序口径。

## 6. UI 需求

### Q：会话卡片如何展示图标？

A：`ChatConversationListPage.threadRow(_:)` 当前左侧仅在有 `latestListImageAttachment` 时显示缩略图。新增图标后建议：

1. 优先展示用户设置的会话图标。
2. 没有设置图标时，使用默认 `bubble.left.circle`。
3. 图标背景使用 `iconColorName` 映射到统一色板。
4. 如果保留最近图片缩略图，建议作为后续增强；第一期不要让图片缩略图和会话图标抢同一个主视觉位。

卡片结构：

```text
[图标]  标题                         时间
        最近消息预览 1~2 行
```

置顶会话建议在标题行展示小图标：

```text
[图标]  标题  [pin.fill]             时间
```

### Q：长按菜单需要哪些项？

A：在 `ChatConversationListPage.threadRow(_:)` 的 `.contextMenu` 中增加：

| 菜单项 | 图标 | 行为 |
| --- | --- | --- |
| 编辑 | `paintbrush` 或 `square.and.pencil` | 打开会话编辑 Sheet |
| 置顶 / 取消置顶 | `pin` / `pin.slash` | 切换 `isPinned`，更新 `pinnedAt` |
| 删除 | `trash` | 保持现有删除逻辑 |

参考旧实现：

```swift
Button {
    // 编辑图标/标题
} label: {
    Label("编辑图标", systemImage: "paintbrush")
}

Button {
    togglePin(record)
} label: {
    Label(record.isPinned ? "取消置顶" : "置顶消息",
          systemImage: record.isPinned ? "pin.slash" : "pin")
}
```

### Q：滑动操作如何设计？

A：保留现有右滑删除，新增左滑快捷操作：

| 方向 | 操作 |
| --- | --- |
| trailing | 删除 |
| leading | 置顶/取消置顶、编辑 |

注意：`allowsFullSwipe` 建议只给删除保持现网体验；leading 侧多个操作不建议 full swipe 直接触发，避免误编辑或误置顶。

### Q：编辑图标页面如何设计？

A：编辑图标页面 **直接按 `hanlin-ai` 的 `IconAndColorPicker` 一比一还原**，不重新设计，不用 SparkClient 现有 `ModelIconPickerSheet` 拼装替代。

源页面位置：

| 旧项目文件 | 范围 | 内容 |
| --- | --- | --- |
| `/Users/hua/Downloads/Reference/hanlin-ai/AI_HLY/Views/Components/ListViewComponents.swift` | `169-284` | `IconAndColorPicker` 页面主体 |
| `/Users/hua/Downloads/Reference/hanlin-ai/AI_HLY/ListView.swift` | `140-159` | Sheet 打开与关闭后回写保存 |
| `/Users/hua/Downloads/Reference/hanlin-ai/AI_HLY/Services/DataServices/InfoServices.swift` | `617-733` | `getIconList()` 图标列表 |
| `/Users/hua/Downloads/Reference/hanlin-ai/AI_HLY/Services/DataServices/InfoServices.swift` | `735-770` | `getColorList()` 颜色列表 |
| `/Users/hua/Downloads/Reference/hanlin-ai/AI_HLY/Services/DataServices/InfoServices.swift` | `773-847` | `Color.from(name:)` 与 `Color.name` 映射 |

一比一还原要求：

1. 页面结构使用 `NavigationStack`。
2. 顶部是预览消息栏：左侧图标、右侧标题输入框和示例消息。
3. 标题输入框 placeholder 保持“请输入标题”。
4. 示例消息文案保持“这是一段示例消息内容...”。
5. 中间是图标选择区，`LazyVGrid` 自适应列，最小宽度 `60`，spacing 与旧页面一致。
6. 底部是颜色选择区，`LazyVGrid` 自适应列，最小宽度 `40`，spacing 与旧页面一致。
7. 图标选中时使用当前 `selectedColor`，未选中为灰色。
8. 颜色选中时用白色描边表示。
9. 卡片背景使用 `.thinMaterial`，圆角 `20`，阴影色按旧页面 `Color.hlBlue` 还原。
10. 导航标题保持“自定义”。
11. 右上角按钮保持“完成”。
12. 默认图标保持 `bubble.left.circle`。
13. 默认颜色保持 `hlBlue`。

第一期字段：

1. 标题：`title`
2. 图标：`iconName`
3. 颜色：`iconColorName`

建议新增：

```swift
ChatThreadAppearanceEditSheet
```

职责：

1. 对外命名可以是 `ChatThreadAppearanceEditSheet`，但内部 UI 需要按旧项目 `IconAndColorPicker` 还原。
2. 接收当前 `ChatThread` 的标题、图标、颜色初始值。
3. 图标和颜色数据源直接迁移旧项目 `getIconList()`、`getColorList()`。
4. 颜色持久化用字符串 key，对齐旧项目 `Color.name` / `Color.from(name:)`。
5. 点击“完成”关闭后回写 `title/iconName/iconColorName`。
6. 如 SparkClient 需要更严格的取消语义，可以外层 Sheet 增加取消按钮；但 `IconAndColorPicker` 页面本体必须先按旧项目一比一还原。

## 7. 业务接口需求

### Q：ViewModel 需要新增哪些方法？

A：建议在 `ChatListViewModel` 增加：

```swift
func updateThreadAppearance(
    threadID: UUID,
    title: String,
    iconName: String?,
    iconColorName: String?
) async

func toggleThreadPinned(_ threadID: UUID) async
```

底层 `ChatRepository` / `ChatThreadStoring` 增加：

```swift
func updateThreadAppearance(
    threadID: UUID,
    title: String,
    iconName: String?,
    iconColorName: String?
) async

func updateThreadPinState(
    threadID: UUID,
    isPinned: Bool,
    pinnedAt: Date?
) async
```

### Q：是否需要单独 UseCase？

A：可以按现有风格选择：

1. 如果当前列表变更都直接走 `ChatListViewModel -> UseCase -> Repository`，则新增 `UpdateChatThreadAppearanceUseCase` 和 `ToggleChatThreadPinUseCase`。
2. 如果项目对线程设置类更新已有直连 repository 先例，也可以第一期放在 ViewModel，但长期建议补 UseCase，避免 `ChatListViewModel` 越来越重。

本需求建议新增 UseCase，因为这两个操作都涉及本地存储、同步状态和列表刷新。

## 8. 涉及文件与改动内容

| 文件 | 改动内容 | 影响 |
| --- | --- | --- |
| `SparkClient/Projects/Core/Domain/Entities/ChatThread.swift` | 增加 `iconName`、`iconColorName`、`isPinned`、`pinnedAt`；处理 Codable 兼容 | 会话领域模型扩展 |
| `SparkClient/Projects/Features/Chat/Domain/ChatStoreProtocols.swift` | 增加更新外观、置顶状态的协议方法 | 存储边界扩展 |
| `SparkClient/Projects/Features/Chat/Infrastructure/CoreDataChatStore.swift` | CoreData 读写新增字段；列表排序改为置顶优先；新增 update 方法 | 本地存储和列表投影变化 |
| `SparkClient/Projects/Features/Chat/Infrastructure/CoreDataChatRepository.swift` | 转发新增 repository 方法 | Repository 门面扩展 |
| `SparkClient/Projects/Features/Chat/Presentation/ChatListViewModel.swift` | 增加编辑外观、置顶/取消置顶方法；搜索结果保持置顶排序 | 列表业务交互扩展 |
| `SparkClient/Projects/Features/Chat/Presentation/ChatConversationListPage.swift` | 行 UI 展示图标；contextMenu 增加编辑/置顶；leading swipe 增加编辑/置顶；弹出编辑 Sheet | 会话列表交互变化 |
| 新增 `ChatThreadAppearanceEditSheet.swift` | 标题、图标、颜色编辑 Sheet | 会话外观编辑入口 |
| CoreData model 文件 | `ChatThreadEntity` 新增字段；全新项目不要求旧库迁移 | 新建本地库 schema 完整 |
| `/Users/hua/Downloads/Reference/SparkService/chat_sync/models.py` | `ChatThread` 增加 `icon_name`、`icon_color_name`、`is_pinned`、`pinned_at` | 服务端线程模型与客户端字段对齐 |
| 服务端线程 DTO/API | 增加 icon/pin 字段的请求和响应映射 | 多端同步一致性 |
| `Localizable.strings` | 新增编辑、置顶、取消置顶、图标、颜色等文案 | 多语言文案 |

## 9. 验收标准

### 功能验收

1. 会话列表每条会话都展示图标；未设置时展示默认图标。
2. 长按会话行出现“编辑、置顶/取消置顶、删除”。
3. leading swipe 出现“置顶/取消置顶、编辑”。
4. trailing swipe 删除保持现网行为。
5. 编辑 Sheet 可修改标题、图标、颜色，保存后列表立即刷新。
6. 取消编辑不写入任何字段。
7. 置顶后该会话移动到列表顶部区域。
8. 取消置顶后该会话回到普通时间排序区域。
9. 搜索列表中仍保持置顶优先。
10. App 重启后标题、图标、颜色、置顶状态保持。

### 数据验收

1. 全新安装/新建本地库时，`ChatThreadEntity` 包含 icon/pin 字段。
2. 本地新建会话写入默认 icon/pin 字段。
3. 服务端 `ChatThread` 模型包含 `icon_name/icon_color_name/is_pinned/pinned_at`。
4. 远端同步回来的会话能恢复 icon/pin 字段。
5. 本地编辑外观和置顶状态能进入同步链路。

### UI 验收

1. 图标尺寸稳定，不因标题长短导致列表行抖动。
2. 长标题单行截断，预览最多两行。
3. 置顶标识不遮挡标题和时间。
4. 深色/浅色模式下图标背景和文字对比度可读。
5. 编辑 Sheet 的保存、取消行为明确。

## 10. 风险与注意事项

### Q：这个需求最大的风险是什么？

A：不是列表 UI，而是数据一致性。`ChatThread` 是客户端、服务端、本地 CoreData、同步 outbox 都会经过的核心模型。只在 UI 层加字段会导致重启丢失、多端不同步或旧数据解码失败。

### Q：标题编辑是否会影响自动标题生成？

A：需要区分“用户手动标题”和“系统默认标题”。如果现有标题会被后续自动摘要覆盖，新增编辑后建议补一个 `titleEditedByUser` 或 `isCustomTitle` 字段。第一期如果不做该字段，就必须明确：用户编辑过标题后，后续自动标题生成不得覆盖。

### Q：置顶是否影响 `updatedAt`？

A：建议置顶更新 `updatedAt` 和本地列表刷新，但排序不要直接依赖 `updatedAt`，否则置顶/取消置顶会改变普通区时间排序。置顶排序用 `pinnedAt`，普通排序用 `latestMessageAt`。

### Q：是否要第一期做颜色自定义？

A：如果颜色选择会拖慢开发，可以第一期只做 `iconName` 和 `isPinned`，`iconColorName` 默认使用主题色。但从用户给出的参考实现看，编辑图标时同时编辑颜色体验更完整，建议纳入同一工单。
