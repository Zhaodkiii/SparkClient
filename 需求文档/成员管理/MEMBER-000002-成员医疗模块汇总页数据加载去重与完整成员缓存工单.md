# MEMBER-000002 成员医疗模块汇总页数据加载去重与完整成员缓存工单

创建日期：2026-06-21

关联文档：

- `MEMBER-000001-成员模块汇总页与分组维护流程工单.md`
- `MEDICAL-000001-用药明细事实源与成员医疗画像投影工单.md`
- `MEDICAL-000002-手术记录成员级事实源与医疗画像投影工单.md`
- `医疗模块引导流程重构详细设计文档.md`
- `医疗模块引导数据存储模型详细设计文档.md`

参考代码：

- `SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/Medical/MemberMedicalModuleSummaryView.swift:1-96`
- `SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/Medical/MemberMedicalModuleSummaryViewModel.swift:33-50`
- `SparkClient/Projects/Core/Networking/API/Medical/MedicalSyncAPI.swift:668-720`
- `SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/MemberModuleSetupView.swift:1-80`

说明：本文档为新增工单需求与详细设计，不直接修改代码。

---

## 一、是否支持该方案

建议支持。

当前问题不是“某个接口慢”，而是页面进入时多个 ViewModel 分别加载同一个成员的医疗资料，导致同一批成员级数据被拆成多次请求：

```text
member-guidance 1 次
nutrition goals 1 次
member-module-settings 1 次
symptoms 1 次
member-medical-profiles 4 次
medication-plans 1 次
surgeries 1 次
```

总计 10 次请求，其中 `member-medical-profiles` 重复 4 次，症状、用药、手术又和 `RemoteMemberCompleteData` 能承载的数据重叠。

更合理的方向是：

```text
以成员为单位加载 RemoteMemberCompleteData
将 MemberMedicalProfile、模块配置、症状、用药计划、手术记录纳入成员完整数据快照
模块汇总页只消费快照，不再各自重复拉列表
用户提交成功后回写快照缓存
```

这样可以把“页面级碎请求”收敛成“成员级快照 + 必要刷新”。

---

## 二、当前问题

### 2.1 进入页面请求次数过多

一次进入成员医疗模块汇总页，日志显示实际请求：

```text
GET /api/v1/medical/member-guidance/?member_id=597                         1次
GET /api/v1/nutrition/goals/?member_id=597                                  1次
GET /api/v1/medical/resources?kind=member-module-settings...                1次
GET /api/v1/medical/resources?kind=symptoms&member_id=597                   1次
GET /api/v1/medical/resources?kind=member-medical-profiles&member_id=597    4次
GET /api/v1/medical/resources?kind=medication-plans&member_id=597           1次
GET /api/v1/medical/resources?kind=surgeries&member_id=597                  1次
```

总计：

```text
10 次请求
```

即使部分是 304，也仍然有网络调度、缓存判断、日志、解码和状态刷新成本。

### 2.2 MemberMedicalModuleSummaryViewModel 内部重复构造引导 ViewModel

当前 `MemberMedicalModuleSummaryViewModel` 初始化时会创建：

```text
MemberMedicalSetupViewModel
```

并在 `loadIfNeeded()` 中：

```text
先请求 sectionProgressMap
再调用 medicalSetupViewModel.loadIfNeeded()
```

这会让“汇总页展示摘要”复用“完整医疗引导流程 ViewModel”的加载逻辑。

问题：

```text
汇总页只需要展示摘要和完成状态
但完整引导 ViewModel 会加载更多表单所需数据
```

因此请求放大。

### 2.3 资源列表和完整成员数据重复

以下列表本质是成员维度数据：

```text
症状记录列表
用药计划列表
手术记录列表
成员医疗档案
模块配置
```

它们不应该在进入汇总页后再分别请求。

更好的方式是作为 `RemoteMemberCompleteData` 的一部分，随成员切换或模块页进入时一次加载。

---

## 三、目标设计

### 3.1 目标一句话

```text
成员医疗模块汇总页以 RemoteMemberCompleteData 作为唯一初始数据快照；
进入选择维护模块后预热一次成员完整数据；
进入医疗模块汇总页时只做缺失补齐或刷新；
所有分组提交成功后回写 RemoteMemberCompleteData 缓存。
```

### 3.2 目标请求数量

理想首进流程：

```text
进入选择维护模块
  -> 加载一次 RemoteMemberCompleteData(member_id)

进入医疗模块汇总页
  -> 如果缓存存在，直接渲染
  -> 如果缺少 memberMedicalProfile 或 moduleSettings，补一次医疗引导汇总/complete-data
```

目标：

```text
从 10 次请求降低到 1-2 次请求
```

### 3.3 数据职责

```text
RemoteMemberCompleteData
    = 成员级医疗数据快照
    = 页面初始渲染和模块汇总页摘要来源

MemberMedicalProfile
    = 成员医疗画像投影
    = 基础档案、健康病史、生活习惯、体检档案、症状/用药/手术摘要

MemberModuleSetting
    = 成员模块开关与分组完成进度

MemberMedicalModuleSummaryViewModel
    = 只从快照推导卡片状态，不单独拉症状/用药/手术列表
```

---

## 四、RemoteMemberCompleteData 扩展

### 4.1 当前模型

当前 `RemoteMemberCompleteData` 已包含：

```text
member
medicalCases
healthExamReports
examinationReports
medicineBoxes
familyMedicineBoxes
prescriptions
medicationPlans
todayMedicationRecords
medicationSummary
symptoms
visits
surgeries
followUps
```

位置：

```text
MedicalSyncAPI.swift:668-720
```

### 4.2 建议新增字段

建议补充：

```swift
/// 成员医疗画像投影；用于基础档案、病史、生活习惯、体检档案等摘要展示，不作为症状/用药/手术明细事实源。
var memberMedicalProfile: RemoteMemberMedicalProfile?
/// 成员模块开关与分组进度；用于判断首页展示哪些模块，以及模块汇总页各分组的完成状态。
var memberModuleSettings: [RemoteMemberModuleSetting]?
```

如果服务端希望按模块返回，也可以：

```swift
var medicalModuleSetting: RemoteMemberModuleSetting?
```

但从扩展性看，建议使用列表：

```text
memberModuleSettings
```

原因：

```text
RemoteMemberCompleteData 是成员完整数据；
一个成员可能同时有 medical、nutrition、daily_health 等模块配置；
列表比单个 medicalModuleSetting 更通用。
```

### 4.2.1 medicalGuidance 是否合并到 memberMedicalProfile

建议合并，不再作为 `RemoteMemberCompleteData` 的独立字段。

当前独立接口：

```text
GET /api/v1/medical/member-guidance/?member_id=597
```

作用：

```text
医疗模块汇总页首页数据
医疗引导分组完成状态
基础档案、健康病史、生活习惯、体检档案、风险评估的摘要
AI 体检计划和风险提示的轻量结果
```

这类数据本质上是“成员医疗模块摘要快照”，和 `memberMedicalProfile`、`symptoms`、`medicationPlans`、`surgeries` 强相关。

如果继续独立请求，会出现：

```text
completeData 有成员明细
member-guidance 又有医疗汇总
页面需要同时理解两套数据
```

因此建议把 `member-guidance` 的输出合并到 `RemoteMemberMedicalProfile` 的结构化投影字段内：

```swift
struct RemoteMemberMedicalProfile: Codable, Sendable, Equatable {
    ...

    /// 医疗引导分组摘要；由服务端基于 profile + 症状/用药/手术/体检档案聚合生成，用于医疗模块汇总页。
    var guidanceSections: [RemoteMemberMedicalProfileSectionSummary]?
    /// 风险评估摘要；用于医疗模块汇总页风险评估卡片和 AI 体检计划上下文。
    var riskAssessmentSummary: String?
    /// 下一次体检计划摘要；用于过往体检档案分组和医疗模块汇总页展示。
    var examPlanSummary: String?
    /// 医疗引导摘要更新时间；用于判断缓存新鲜度和调试数据来源。
    var guidanceUpdatedAt: Date?
}
```

目标用途：

```text
MemberMedicalModuleSummaryView 优先从 completeData.memberMedicalProfile.guidanceSections 渲染卡片摘要
风险评估读取 completeData.memberMedicalProfile.riskAssessmentSummary
下一次体检计划读取 completeData.memberMedicalProfile.examPlanSummary
只有 memberMedicalProfile 缺失或过期时，才补齐一次 profile/guidance 数据
```

建议结构：

```swift
struct RemoteMemberMedicalProfileSectionSummary: Codable, Sendable, Equatable {
    var sectionCode: String
    var title: String
    var summary: String
    var status: String
}
```

设计边界：

```text
Symptom / MedicationPlan / Surgery / HealthExamReport 仍然是明细事实源。
RemoteMemberMedicalProfile 内的 guidanceSections / riskAssessmentSummary / examPlanSummary 只是画像投影。
客户端不要同时维护 RemoteMemberMedicalGuidance 和 RemoteMemberMedicalProfile 两套医疗摘要模型。
```

### 4.2.2 nutritionGoalState 是否放入 RemoteMemberCompleteData

已确认放入 `RemoteMemberCompleteData`。

当前独立接口：

```text
GET /api/v1/nutrition/goals/?member_id=597
```

作用：

```text
饮食健康模块基础信息
身高、体重、目标模式、热量目标、营养目标
饮食模块汇总页完成状态和摘要
医疗基础档案中身高/体重的可复用来源
```

这个接口不属于医疗模块本身，但属于“成员建档模块汇总页”的初始数据。

如果选择维护模块页面需要同时判断：

```text
医疗模块完成度
饮食健康完成度
```

那么饮食目标也应该随着成员完整快照一起预加载。

建议在 `RemoteMemberCompleteData` 内新增：

```swift
var nutritionGoalState: RemoteNutritionGoalState?
```

用途：

```text
MemberNutritionModuleSummaryView 使用 nutritionGoalState 渲染基础信息卡片
MemberMedicalSetupSheet 的身高/体重默认值可以复用 nutritionGoalState.goal
避免进入医疗页时再额外请求 nutrition goals
```

注意：

```text
医疗汇总页不应为了渲染医疗卡片主动请求 nutrition goals。
如果 completeData 内有 nutritionGoalState，可作为身高/体重补充来源。
如果没有，医疗页仍应正常工作。
nutrition goals 虽来自独立领域，但在成员建档入口允许作为 completeData 的聚合子节点返回。
```

### 4.3 建议目标结构

```swift
struct RemoteMemberCompleteData: Codable, Sendable, Equatable {
    var memberId: Int
    var member: RemoteMember

    /// 成员医疗画像投影；用于基础档案、病史、生活习惯、体检档案等摘要展示，不作为症状/用药/手术明细事实源。
    var memberMedicalProfile: RemoteMemberMedicalProfile?
    /// 成员模块开关与分组进度；用于判断首页展示哪些模块，以及模块汇总页各分组的完成状态。
    var memberModuleSettings: [RemoteMemberModuleSetting]?
    /// 饮食目标轻量快照；用于饮食模块汇总页基础信息展示，也可为医疗基础档案提供身高/体重初始值。
    var nutritionGoalState: RemoteNutritionGoalState?

    var symptoms: [RemoteSymptom]?
    var medicationPlans: [RemoteMedicationPlan]?
    var surgeries: [RemoteSurgery]?

    var medicalCases: [RemoteMedicalCaseSummary]?
    var healthExamReports: [RemoteHealthExamReportWithAttachments]?
    var examinationReports: [RemoteExaminationReportWithAttachments]?
    var medicineBoxes: [RemoteMedicineBox]?
    var prescriptions: [RemotePrescription]?
    var followUps: [RemoteFollowUp]?
}
```

### 4.4 编码解码要求

继续使用项目统一编码解码：

```text
JSONDecoder.default
JSONEncoder.default
convertToSnakeCase
ISO8601 日期
```

不要为这些字段手写 `CodingKeys`。

---

## 五、服务端接口设计

### 5.1 complete-data 增强

目标：

```text
GET /api/v1/medical/members/{member_id}/complete-data/
```

或现有 complete-data 接口按当前路径保持不变。

需要返回：

```text
member_medical_profile
member_module_settings
symptoms
medication_plans
surgeries
```

其中：

```text
symptoms / medication_plans / surgeries
```

用于医疗模块汇总页和分组页初始展示，不再进入页面后单独拉。

### 5.2 member-guidance 接口定位

当前：

```text
GET /api/v1/medical/member-guidance/?member_id=597
```

建议定位：

```text
医疗引导聚合接口
用于进入医疗模块汇总页时补齐 memberMedicalProfile 与分组摘要
```

但如果 `complete-data` 已能返回全部引导所需字段，则 `member-guidance` 可以减少使用，避免成为第二套事实快照。

### 5.2.1 member-guidance 合入 complete-data 的目标

建议将 `member-guidance` 的结果合并进 complete-data 内的 `member_medical_profile` 返回：

```text
RemoteMemberCompleteData.memberMedicalProfile.guidanceSections
RemoteMemberCompleteData.memberMedicalProfile.riskAssessmentSummary
RemoteMemberCompleteData.memberMedicalProfile.examPlanSummary
RemoteMemberCompleteData.memberMedicalProfile.guidanceUpdatedAt
```

服务端策略：

```text
complete-data 内部复用 member-guidance 的构建逻辑
不要在客户端进入页面后再并发请求 member-guidance
member-guidance 可以保留为调试/fallback 接口，但响应结构应与 memberMedicalProfile 内的投影字段保持一致
```

响应示例：

```json
{
  "member_id": 597,
  "member": {},
  "member_medical_profile": {
    "guidance_sections": [
      {
        "section_code": "basic_profile",
        "title": "基础档案",
        "summary": "女 · 58岁 · 160cm · 60kg",
        "status": "completed"
      },
      {
        "section_code": "health_history",
        "title": "健康病史与症状记录",
        "summary": "发热 · 二甲双胍 · 阑尾切除术",
        "status": "completed"
      }
    ],
    "risk_assessment_summary": "心血管风险中等",
    "exam_plan_summary": "建议年度体检并复查血糖",
    "guidance_updated_at": "2026-06-21T10:00:00Z"
  },
  "member_module_settings": []
}
```

客户端使用优先级：

```text
优先 completeData.memberMedicalProfile.guidanceSections
其次从 memberMedicalProfile + symptoms + medicationPlans + surgeries 本地推导
最后才补请求 member-guidance
```

### 5.2.2 nutrition goals 合入 complete-data 的目标

确认合入服务端 complete-data。

```text
RemoteMemberCompleteData.nutritionGoalState
```

服务端策略：

```text
如果 SparkService 与 nutrition 模块同服务同库：
    complete-data 直接聚合 nutrition goal

如果 nutrition 未来独立服务：
    可由服务端聚合层或网关继续维持该字段，对客户端保持稳定
```

响应示例：

```json
{
  "nutrition_goal_state": {
    "member_id": 597,
    "goal": {
      "goal_type": "maintain",
      "height_cm": "160.00",
      "current_weight_kg": "60.00",
      "daily_energy_target_kcal": "1995.00",
      "carbohydrate_target_g": "244.39",
      "protein_target_g": "99.75",
      "fat_target_g": "55.42"
    },
    "defaults": {
      "goal_type": "maintain",
      "energy_kcal": 1995
    }
  }
}
```

客户端使用优先级：

```text
饮食汇总页：
    优先 completeData.nutritionGoalState
    缺失时补请求 /api/v1/nutrition/goals/

医疗基础档案：
    如果 completeData.nutritionGoalState.goal 有 height/currentWeight
    则作为身高/体重初始值来源
    不因为缺失 nutritionGoalState 阻塞医疗流程
```

### 5.3 模块配置列表纳入 complete-data

当前单独请求：

```text
GET /api/v1/medical/resources?kind=member-module-settings&member_id=597&module_code=medical
```

目标：

```text
RemoteMemberCompleteData.memberModuleSettings
```

切换成员或进入选择维护模块时一起加载。

医疗模块汇总页从缓存读取：

```text
memberModuleSettings.first(where: moduleCode == "medical")
```

### 5.4 不再单独加载的资源

医疗模块汇总页不再单独请求：

```text
GET /api/v1/medical/resources?kind=symptoms&member_id=597
GET /api/v1/medical/resources?kind=medication-plans&member_id=597
GET /api/v1/medical/resources?kind=surgeries&member_id=597
GET /api/v1/medical/resources?kind=member-medical-profiles&member_id=597
```

改为：

```text
从 RemoteMemberCompleteData 读取
```

只有用户进入某个分组详情且需要分页/深加载时，才按需请求单独列表。

---

## 六、客户端加载流程

### 6.1 进入选择维护模块页

位置：

```text
MemberModuleSetupView.swift
```

目标：

```text
进入页面后加载一次当前成员 RemoteMemberCompleteData
```

流程：

```text
MemberModuleSetupView.onAppear / task
    -> flowViewModel.preloadMemberCompleteDataIfNeeded(memberID)
    -> 写入 MemberCompleteDataStore / memberContextStore 缓存
```

UI：

```text
如果正在加载：
    可展示轻量 loading

如果已有缓存：
    先展示缓存，再后台刷新
```

### 6.2 进入医疗模块汇总页

位置：

```text
MemberMedicalModuleSummaryView
```

目标：

```text
优先读取 RemoteMemberCompleteData 缓存
如果缓存不存在或缺少医疗 profile，再加载一次医疗汇总数据
```

加载策略：

```text
1. 从缓存读取 completeData(memberID)
2. 如果有 memberMedicalProfile + moduleSettings：
      直接 rebuildSections()
3. 如果缺字段：
      显示“正在加载医疗资料”
      请求 complete-data 或 profile 补齐接口
      成功后写入缓存
      rebuildSections()
```

页面文案：

```text
正在加载医疗资料
```

### 6.3 分组提交成功后

用户在任意分组 sheet 中保存成功：

```text
服务端返回最新 MemberMedicalProfile / moduleSetting / mutation response
```

客户端必须：

```text
更新 RemoteMemberCompleteData.memberMedicalProfile
更新 RemoteMemberCompleteData.memberModuleSettings
按 mutation 类型更新 symptoms / medicationPlans / surgeries
刷新医疗模块汇总页 sections
```

示例：

```text
保存症状成功：
    applySymptomMutation(response)
    completeData.symptoms = 更新后的症状列表
    completeData.memberMedicalProfile.symptomFollowUpFocus = response.memberProfile.symptomFollowUpFocus

保存用药成功：
    applyMedicationMutation(response)
    completeData.medicationPlans = 更新后的用药计划
    completeData.memberMedicalProfile.medicationFocus = response.memberProfile.medicationFocus

保存手术成功：
    applySurgeryMutation(response)
    completeData.surgeries = 更新后的手术记录
    completeData.memberMedicalProfile.surgeryFocus = response.memberProfile.surgeryFocus
```

---

## 七、缓存设计

### 7.1 缓存对象

建议使用已有成员完整数据缓存：

```text
MemberCompleteDataStore
```

如果当前项目中命名不同，以实际已有 store 为准，不新增平行缓存。

### 7.2 缓存 key

```text
memberID
```

### 7.3 缓存写入时机

```text
选择维护模块页预加载成功
医疗模块汇总页补齐加载成功
分组保存成功
症状/用药/手术 mutation 成功
模块配置保存成功
```

### 7.4 缓存读取时机

```text
MemberModuleSetupView
MemberMedicalModuleSummaryView
MemberMedicalSetupSheetView 分组模式
MemberNutritionModuleSummaryView
首页成员模块摘要
```

### 7.5 缓存更新原则

```text
局部 mutation 优先局部更新缓存
完整刷新成功后整体覆盖缓存
缓存存在时先展示，再后台刷新
```

避免：

```text
每次回到页面都重新请求所有 kind
```

---

## 八、MemberMedicalModuleSummaryViewModel 调整

### 8.1 当前问题

当前 ViewModel：

```text
初始化 MemberMedicalSetupViewModel
loadIfNeeded 中调用 medicalSetupViewModel.loadIfNeeded()
```

这会让汇总页加载完整引导 ViewModel 的数据。

### 8.2 目标职责

`MemberMedicalModuleSummaryViewModel` 只做：

```text
读取 completeData 缓存
读取 moduleSetting / sectionProgress
推导 sections
打开分组 sheet
完成/跳过模块
```

不再直接拥有完整 `MemberMedicalSetupViewModel`。

### 8.3 建议输入

```swift
init(
    member: Member,
    flowViewModel: MemberSetupFlowViewModel,
    completeDataStore: MemberCompleteDataStore
)
```

或通过：

```text
flowViewModel.homeDependencies
```

获取统一 store。

### 8.4 section 推导来源

```text
基础档案
    -> completeData.member + completeData.memberMedicalProfile

健康病史与症状记录
    -> completeData.memberMedicalProfile
    -> completeData.symptoms
    -> completeData.medicationPlans
    -> completeData.surgeries

生活习惯
    -> completeData.memberMedicalProfile.extra

过往体检档案
    -> completeData.memberMedicalProfile
    -> completeData.healthExamReports
    -> completeData.examinationReports

风险评估
    -> completeData.memberMedicalProfile.riskAssessmentSummary
    -> completeData.memberMedicalProfile.extra.risk_assessment
    -> AI 结果缓存

饮食健康基础信息
    -> completeData.nutritionGoalState
    -> nutrition goals 补齐请求
```

---

## 九、页面 UI

### 9.1 有缓存时

```text
┌────────────────────────────────────┐
│ ←              医疗模块             │
├────────────────────────────────────┤
│ ❤ 医疗模块                          │
│ 为妈妈完善医疗健康档案               │
│ 已完成 2 / 5                        │
├────────────────────────────────────┤
│ ▶ 开始全部流程                  >   │
├────────────────────────────────────┤
│ 👤 基础档案              已完成  >   │
│ 女 · 58岁 · 160cm · 60kg            │
├────────────────────────────────────┤
│ 🩺 健康病史与症状记录     已完成  >   │
│ 发热 · 二甲双胍 · 阑尾切除术         │
├────────────────────────────────────┤
│ 🏃 生活习惯              未完成  >   │
│ 待补充                              │
├────────────────────────────────────┤
│ 📄 过往体检档案          已完成  >   │
│ 2025年体检 · 血糖偏高               │
├────────────────────────────────────┤
│ ⚠ 风险评估              未开始  >   │
│ 待生成                              │
└────────────────────────────────────┘
```

### 9.2 无缓存加载中

```text
┌────────────────────────────────────┐
│ ←              医疗模块             │
├────────────────────────────────────┤
│ ❤ 医疗模块                          │
│ 为妈妈完善医疗健康档案               │
├────────────────────────────────────┤
│                                    │
│          正在加载医疗资料           │
│                                    │
└────────────────────────────────────┘
```

### 9.3 加载失败

```text
┌────────────────────────────────────┐
│ ←              医疗模块             │
├────────────────────────────────────┤
│ 医疗资料加载失败                     │
│ 网络异常或服务暂不可用               │
│                                    │
│              重试                   │
└────────────────────────────────────┘
```

### 9.4 有缓存但刷新失败

```text
继续展示缓存内容
顶部或轻提示显示：医疗资料暂未刷新
```

不要因为后台刷新失败清空页面。

---

## 十、请求去重策略

### 10.1 页面级 in-flight 去重

同一 memberID 同一时间只能有一个 completeData 加载任务。

```text
如果已有请求进行中：
    复用当前 Task
```

### 10.2 缓存命中策略

```text
缓存存在且字段完整：
    直接渲染

缓存存在但缺字段：
    渲染已有内容 + 后台补齐

缓存不存在：
    显示 loading + 请求
```

### 10.3 禁止重复 kind 请求

医疗模块汇总页不应再发：

```text
kind=symptoms
kind=medication-plans
kind=surgeries
kind=member-medical-profiles
```

除非用户进入对应详情页且需要分页/深加载。

---

## 十一、提交成功后的缓存同步

### 11.1 MemberMedicalProfile 保存成功

```text
服务端返回 RemoteMemberMedicalProfile
客户端写入 completeData.memberMedicalProfile
```

### 11.2 模块配置保存成功

```text
服务端返回 RemoteMemberModuleSetting
客户端更新 completeData.memberModuleSettings
```

### 11.3 症状 mutation 成功

```text
服务端返回 SymptomMutationResponse
客户端：
    更新 completeData.symptoms
    更新 completeData.memberMedicalProfile
    rebuildSections()
```

### 11.4 用药 mutation 成功

```text
服务端返回 MedicationMutationResponse
客户端：
    更新 completeData.medicationPlans
    更新 completeData.memberMedicalProfile
    rebuildSections()
```

### 11.5 手术 mutation 成功

```text
服务端返回 SurgeryMutationResponse
客户端：
    更新 completeData.surgeries
    更新 completeData.memberMedicalProfile
    rebuildSections()
```

### 11.6 医疗 profile/guidance 投影刷新成功

```text
服务端返回 RemoteMemberMedicalProfile
客户端：
    更新 completeData.memberMedicalProfile
    rebuildSections()
```

### 11.7 营养目标保存成功

```text
服务端返回 RemoteNutritionGoalState 或 RemoteNutritionGoal
客户端：
    更新 completeData.nutritionGoalState
    刷新饮食模块汇总页基础信息卡片
    如果医疗基础档案还未确认身高/体重，可同步作为初始值
```

---

## 十二、验收标准

### 12.1 请求数量

进入选择维护模块页：

```text
最多 1 次成员 completeData 加载
如果 completeData.memberMedicalProfile 已包含 guidanceSections，则不再额外请求 member-guidance
如果 completeData 已包含 nutritionGoalState，则不再额外请求 nutrition goals
```

进入医疗模块汇总页：

```text
缓存完整：0 次网络请求
缓存缺失：最多 1 次补齐请求
```

不再出现：

```text
member-medical-profiles 重复 4 次
symptoms / medication-plans / surgeries 在汇总页重复单独加载
member-guidance 与 completeData 同时重复拉取同一份医疗汇总
nutrition goals 在选择维护模块页和医疗汇总页重复拉取
```

### 12.2 页面展示

1. 有缓存时医疗模块汇总页立即展示卡片。
2. 无缓存时展示“正在加载医疗资料”。
3. 加载成功后写入缓存并刷新卡片。
4. 加载失败时展示错误和重试。
5. 有旧缓存但刷新失败时继续展示旧缓存。

### 12.3 数据一致性

1. 保存基础档案后，`completeData.memberMedicalProfile` 更新。
2. 新增/编辑/删除症状后，`completeData.symptoms` 与 profile 投影更新。
3. 新增/编辑/删除用药后，`completeData.medicationPlans` 与 profile 投影更新。
4. 新增/编辑/删除手术后，`completeData.surgeries` 与 profile 投影更新。
5. 返回医疗模块汇总页后，不再重新触发 10 次请求。

---

## 十三、阶段拆分

### 阶段一：数据模型扩展

1. `RemoteMemberCompleteData` 增加 `memberMedicalProfile`。
2. `RemoteMemberCompleteData` 增加 `memberModuleSettings`。
3. `RemoteMemberMedicalProfile` 增加 `guidanceSections`、`riskAssessmentSummary`、`examPlanSummary`、`guidanceUpdatedAt`。
4. `RemoteMemberCompleteData` 增加 `nutritionGoalState`。
5. 服务端 complete-data 返回上述字段；不新增顶层 `medicalGuidance` 字段。

### 阶段二：缓存与预加载

1. 选择维护模块页进入时预加载当前成员 completeData。
2. completeData 成功后写入缓存。
3. 同 memberID 请求做 in-flight 去重。

### 阶段三：医疗模块汇总页去重

1. `MemberMedicalModuleSummaryViewModel` 不再构造完整 `MemberMedicalSetupViewModel` 作为加载来源。
2. 从 completeData 推导 sections。
3. 缺字段时只补齐一次。

### 阶段四：保存后回写缓存

1. 基础档案保存后更新 profile 缓存。
2. 症状 mutation 后更新 symptoms/profile 缓存。
3. 用药 mutation 后更新 medicationPlans/profile 缓存。
4. 手术 mutation 后更新 surgeries/profile 缓存。
5. 模块配置保存后更新 memberModuleSettings 缓存。

---

## 十四、已确认方案与剩余问题

以下问题已确认部分方案；实现时按确认方案执行，不再作为开放问题处理。

### 问题一：member-guidance 是否保留为独立接口？

确认方案：方案 B。

```text
member-guidance 的聚合结果合并进 completeData.memberMedicalProfile。
MemberMedicalModuleSummaryView 只读 completeData.memberMedicalProfile.guidanceSections。
不再新增 RemoteMemberCompleteData.medicalGuidance 顶层字段。
```

方案 A：保留 `member-guidance`

```text
completeData 负责成员全量资源
member-guidance 负责医疗引导聚合摘要
医疗模块汇总页缺少 profile/summary 时调用 member-guidance
```

优点：

```text
改动较小
医疗引导汇总逻辑可以独立演进
```

缺点：

```text
仍然存在 completeData 与 member-guidance 两套聚合模型
需要严格规定谁是缓存事实源
```

方案 B：把 member-guidance 合并进 completeData

```text
completeData 返回医疗引导所需全部摘要
医疗模块汇总页只读 completeData
```

优点：

```text
请求最少
缓存事实源最清晰
```

缺点：

```text
服务端 completeData 变重
后续字段增多时需要注意懒加载边界
```

修订建议：

```text
已确认选择方案 B：
将 member-guidance 的聚合结果合并到 completeData.memberMedicalProfile 返回。

为了降低改动风险，可以保留 member-guidance 路径作为 fallback；
但 MemberMedicalModuleSummaryView 首选 completeData.memberMedicalProfile.guidanceSections。
```

### 问题二：module settings 放 completeData 还是独立接口？

确认方案：方案 A。

```text
module settings 放入 RemoteMemberCompleteData.memberModuleSettings。
进入选择维护模块页、切换成员、医疗/饮食模块汇总页均优先读取 completeData.memberModuleSettings。
不再由 MemberMedicalModuleSummaryView 单独请求 member-module-settings。
```

方案 A：放入 `RemoteMemberCompleteData.memberModuleSettings`

优点：

```text
切换成员和进入建档页时一次拿齐
医疗/饮食/日常健康都能复用
```

缺点：

```text
completeData 需要关心模块配置
```

方案 B：继续独立接口，但由 `MemberModuleSetupView` 统一加载一次

优点：

```text
边界更清晰
服务端改动较少
```

缺点：

```text
仍多一次请求
需要额外缓存 module settings
```

建议：

```text
已确认选择方案 A。
模块配置是成员首页展示和建档流程的基础状态，放入成员完整快照更自然。
```

### 问题三：completeData 是否默认返回症状、用药、手术全量？

当前确认：客户端与服务端现状均为方案 B。

客户端现状：

```text
RemoteMemberCompleteData.symptoms          -> [RemoteSymptom]?
RemoteMemberCompleteData.medicationPlans   -> [RemoteMedicationPlan]?
RemoteMemberCompleteData.surgeries         -> [RemoteSurgery]?
```

服务端现状：

```text
MemberCompleteDataAPI
    symptoms         -> SymptomSerializer(symptoms, many=True)
    medicationPlans  -> MedicationPlanSerializer(medication_plans, many=True)
    surgeries        -> SurgerySerializer(surgeries, many=True)
```

因此当前不是轻量摘要 DTO，而是直接返回完整远端模型。

方案 A：默认返回轻量列表

```text
symptoms / medicationPlans / surgeries 返回摘要字段
详情页再深加载
```

优点：

```text
汇总页够用
数据量可控
```

缺点：

```text
详情页可能仍需二次请求
```

方案 B：默认返回完整列表

优点：

```text
详情页打开更快
```

缺点：

```text
成员数据多时 completeData 过重
```

建议：

```text
已确认当前实现为方案 B，本工单先沿用方案 B。
不要在本工单内额外新增 SymptomSummary / MedicationPlanSummary / SurgerySummary DTO。
后续如果成员数据量明显增大，再单独创建“completeData 轻量化”优化工单。
```

### 问题四：nutrition goals 是否应该放入 RemoteMemberCompleteData？

确认方案：方案 A。

```text
nutrition goals 放入 RemoteMemberCompleteData.nutritionGoalState。
选择维护模块页一次拿到医疗与饮食两个模块的初始状态。
医疗基础档案可直接复用身高/体重，不再依赖额外客户端组合缓存作为主链路。
```

方案 A：放入 `RemoteMemberCompleteData.nutritionGoalState`

```text
选择维护模块页一次拿到医疗与饮食两个模块的初始状态
饮食汇总页不再额外请求 nutrition goals
医疗基础档案可复用身高/体重
```

优点：

```text
请求最少
模块选择页完成度判断最直接
身高/体重跨医疗和饮食复用更自然
```

缺点：

```text
RemoteMemberCompleteData 会跨到营养领域
如果 nutrition 未来独立服务，服务端聚合边界会变重
```

方案 B：不放入 completeData，但由 MemberModuleSetupView 统一加载并写入同一缓存上下文

```text
completeData 仍只负责医疗资源
nutrition goals 由模块选择页统一预热
缓存层把 completeData + nutritionGoalState 组合给页面使用
```

优点：

```text
服务端边界清晰
nutrition API 可独立演进
```

缺点：

```text
仍会多一次接口
客户端缓存组合逻辑更复杂
```

建议：

```text
已确认选择方案 A。
nutrition goals 作为 RemoteMemberCompleteData 的可选子节点返回。
客户端仍可在极端失败场景下单独补请求 /api/v1/nutrition/goals/，但主链路按 completeData 读取。
```

---

## 十五、实现落地细节

### 15.1 服务端改造清单

涉及项目：

```text
/Users/hua/Downloads/Reference/SparkService
```

建议改造文件：

```text
medical/views.py
medical/serializers.py
medical/services/member_medical_profile_service.py
medical/tests.py
```

#### 15.1.1 complete-data 响应增加医疗画像与模块配置

目标接口：

```text
GET /api/v1/medical/members/{member_id}/complete-data/
```

当前接口已经返回：

```text
member
medical_cases
health_exam_reports
examination_reports
medicine_boxes
prescriptions
medication_plans
today_medication_records
medication_summary
symptoms
visits
surgeries
follow_ups
```

需要新增：

```text
member_medical_profile
member_module_settings
```

不要新增：

```text
medical_guidance
nutrition_goal_state
```

服务端响应目标：

```json
{
  "member_id": 597,
  "member": {},
  "member_medical_profile": {
    "id": 12,
    "member": 597,
    "symptom_follow_up_focus": ["发热", "咳嗽"],
    "medication_focus": [
      {
        "drug_name": "二甲双胍",
        "summary": "每日2次",
        "status": "active",
        "source_plan_id": 10
      }
    ],
    "surgery_focus": [],
    "guidance_sections": [
      {
        "section_code": "basic_profile",
        "title": "基础档案",
        "summary": "女 · 58岁 · 160cm · 60kg",
        "status": "completed"
      }
    ],
    "risk_assessment_summary": "心血管风险中等",
    "exam_plan_summary": "建议年度体检并复查血糖",
    "guidance_updated_at": "2026-06-21T10:00:00Z"
  },
  "member_module_settings": [
    {
      "id": 1,
      "member": 597,
      "module_code": "medical",
      "is_enabled": true,
      "is_completed": false,
      "display_order": 10
    }
  ],
  "symptoms": [],
  "medication_plans": [],
  "surgeries": []
}
```

#### 15.1.2 MemberMedicalProfileSerializer 字段补齐

`MemberMedicalProfileSerializer` 需要支持：

```text
guidance_sections
risk_assessment_summary
exam_plan_summary
guidance_updated_at
```

建议字段边界：

```text
guidance_sections：服务端运行时聚合出来的分组摘要，可落库也可 serializer 动态计算
risk_assessment_summary：风险评估轻量摘要，可优先来自 profile.extra 或 AI 结果缓存
exam_plan_summary：下一次体检计划摘要，可优先来自 task_system 或 profile.extra
guidance_updated_at：该投影最近更新时间
```

如果短期不想改表结构，可以先放入 `MemberMedicalProfile.extra`，但 serializer 对客户端输出必须是结构化字段：

```text
extra.guidance_sections          -> guidance_sections
extra.risk_assessment_summary    -> risk_assessment_summary
extra.exam_plan_summary          -> exam_plan_summary
extra.guidance_updated_at        -> guidance_updated_at
```

注意：

```text
客户端不直接解析 extra 内这些字段作为主路径。
extra 只作为服务端兼容存储，API 输出必须结构化。
```

#### 15.1.3 医疗引导聚合逻辑复用

`member-guidance` 现有聚合逻辑不要复制一份。

建议抽出服务：

```python
def build_member_medical_guidance_projection(member, profile, *, symptoms, medication_plans, surgeries, health_exam_reports, examination_reports) -> dict:
    ...
```

返回：

```python
{
    "guidance_sections": [...],
    "risk_assessment_summary": "...",
    "exam_plan_summary": "...",
    "guidance_updated_at": timezone.now(),
}
```

使用位置：

```text
MemberCompleteDataAPI
member-guidance fallback API
profile mutation 后需要重算的地方
```

#### 15.1.4 module settings 查询

`MemberCompleteDataAPI` 内直接查询：

```python
MemberModuleSetting.objects.filter(member_id=member_id).order_by("display_order", "id")
```

序列化后返回：

```text
member_module_settings
```

如果没有设置记录：

```text
返回空数组 []
不要返回 null
```

原因：

```text
客户端可以直接判断 []，避免 nil 与空数组双分支。
```

#### 15.1.5 complete-data ETag 需要纳入新增数据

当前 ETag 计算需要补充：

```text
member_medical_profile.updated_at
member_module_settings.id/updated_at
```

建议 ETag 组成：

```text
member
medical_cases
health_exam_reports
examination_reports
medicine_boxes
prescriptions
medication_plans
today_medication_records
symptoms
visits
surgeries
follow_ups
member_medical_profile
member_module_settings
```

否则会出现：

```text
保存模块设置成功
客户端重新请求 complete-data
服务端仍返回 304
客户端拿不到最新模块状态
```

#### 15.1.6 服务端日志

关键日志建议：

```text
complete-data 开始构建 member_id=597
complete-data 命中权限 member_id=597 user_id=265
complete-data 聚合完成 profile=1 moduleSettings=2 symptoms=3 medicationPlans=4 surgeries=1
complete-data guidance 投影完成 sections=5 riskSummary=1 examPlan=1
complete-data ETag 命中/未命中 member_id=597 etag=...
```

### 15.2 客户端改造清单

涉及项目：

```text
/Users/hua/Downloads/Reference/SparkClient
```

建议改造文件：

```text
SparkClient/Projects/Core/Networking/API/Medical/MedicalSyncAPI.swift
SparkClient/Projects/Core/Networking/API/Medical/MedicalQueryAPI.swift
SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/MemberModuleSetupView.swift
SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/MemberSetupFlowViewModel.swift
SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/Medical/MemberMedicalModuleSummaryView.swift
SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/Medical/MemberMedicalModuleSummaryViewModel.swift
SparkClient/Projects/Features/Home/Presentation/Members/MemberSetup/Nutrition/...
SparkClient/Projects/Features/Home/Presentation/HomeViewModel.swift
```

#### 15.2.1 Remote DTO 扩展

`RemoteMemberCompleteData` 增加：

```swift
/// 成员医疗画像投影；用于医疗模块汇总页、分组完成状态、AI 风险摘要和体检计划摘要。
var memberMedicalProfile: RemoteMemberMedicalProfile?

/// 成员模块配置列表；用于判断首页展示哪些模块，以及医疗/饮食模块汇总页完成状态。
var memberModuleSettings: [RemoteMemberModuleSetting]?
```

`RemoteMemberCompleteData` 不增加：

```swift
var medicalGuidance: RemoteMemberMedicalGuidance?
```

`RemoteMemberCompleteData` 增加：

```swift
/// 饮食目标轻量快照；用于饮食模块汇总页基础信息展示，也可为医疗基础档案提供身高/体重初始值。
var nutritionGoalState: RemoteNutritionGoalState?
```

`RemoteMemberMedicalProfile` 增加：

```swift
/// 医疗引导分组摘要；服务端由 profile + 症状/用药/手术/体检档案聚合生成。
var guidanceSections: [RemoteMemberMedicalProfileSectionSummary]?

/// 风险评估摘要；用于医疗模块汇总页风险卡片。
var riskAssessmentSummary: String?

/// 下一次体检计划摘要；用于体检档案分组和计划卡片。
var examPlanSummary: String?

/// 医疗引导摘要更新时间；用于缓存新鲜度判断和调试。
var guidanceUpdatedAt: Date?
```

新增：

```swift
struct RemoteMemberMedicalProfileSectionSummary: Codable, Sendable, Equatable, Identifiable {
    var sectionCode: String
    var title: String
    var summary: String
    var status: String

    var id: String { sectionCode }
}
```

编码解码要求：

```text
必须继续使用项目统一 JSONEncoder.default / JSONDecoder.default。
字段名使用 Swift camelCase，依赖 snake_case 策略自动映射。
不要手写 CodingKeys，除非字段确实和默认策略不一致。
```

#### 15.2.2 客户端缓存上下文

如页面实现上需要统一持有页面态，可选增加一个客户端缓存上下文：

```swift
struct MemberModuleSetupCacheContext: Sendable, Equatable {
    var memberID: Int
    var completeData: SparkMedicalSyncAPI.RemoteMemberCompleteData?
    var nutritionGoalState: RemoteNutritionGoalState?
    var loadedAt: Date?
}
```

职责：

```text
completeData：来自 MedicalQueryAPI.fetchMemberCompleteData
nutritionGoalState：优先来自 completeData.nutritionGoalState；缺失时再走 NutritionAPI.fetchGoalState
```

不要把该结构误认为服务端 DTO；它只是页面状态封装。

建议放置位置：

```text
MemberSetup/Routing 或 MemberSetup/MemberModuleSetupCacheContext.swift
```

#### 15.2.3 MemberModuleSetupView 加载策略

进入选择维护模块页时：

```swift
let completeData = try await medicalQueryAPI.fetchMemberCompleteData(memberID: member.id)
```

处理规则：

```text
completeData 成功：写入 MemberModuleSetupCacheContext.completeData
如果 completeData.nutritionGoalState 存在：直接写入 MemberModuleSetupCacheContext.nutritionGoalState
如果 completeData.nutritionGoalState 缺失：可选补请求 nutrition goals，并回写缓存
completeData 失败：医疗模块显示加载失败/可重试，但页面不崩溃
```

并发请求要求：

```text
同 memberID 进入同一页面，只允许一个 in-flight completeData 请求。
只有 completeData 缺失 nutritionGoalState 时，才允许一个 in-flight nutrition goals 补请求。
重复进入页面优先使用缓存，再后台刷新。
```

#### 15.2.4 MemberMedicalModuleSummaryViewModel 数据源

禁止：

```text
进入 MemberMedicalModuleSummaryView 后再次并发请求：
- member-module-settings
- member-medical-profiles
- symptoms
- medication-plans
- surgeries
- member-guidance
```

允许：

```text
completeData 不存在时，补一次 fetchMemberCompleteData
completeData.memberMedicalProfile 缺少 guidanceSections 时，补一次 fetchMemberCompleteData 或 profile 补齐接口
```

推荐初始化：

```swift
init(
    memberID: Int,
    cacheContext: MemberModuleSetupCacheContext?,
    dependencies: HomeFeatureDependencies
)
```

构建 sections 的优先级：

```text
1. completeData.memberMedicalProfile.guidanceSections
2. completeData.memberMedicalProfile + completeData.symptoms + completeData.medicationPlans + completeData.surgeries 本地推导
3. 缺失时展示加载态并补 completeData
```

#### 15.2.5 分组卡片状态计算

分组状态来源：

```text
基础档案：
    member + memberMedicalProfile

健康病史与症状记录：
    memberMedicalProfile.symptomFollowUpFocus
    memberMedicalProfile.medicationFocus
    memberMedicalProfile.surgeryFocus
    symptoms / medicationPlans / surgeries

生活习惯：
    memberMedicalProfile.extra

过往体检档案：
    healthExamReports
    examinationReports
    memberMedicalProfile.examPlanSummary

风险评估：
    memberMedicalProfile.riskAssessmentSummary
```

状态枚举建议：

```swift
enum MemberMedicalModuleSectionStatus: String, Codable, Sendable {
    case notStarted = "not_started"
    case inProgress = "in_progress"
    case completed = "completed"
    case skipped = "skipped"
}
```

如果服务端 `status` 是字符串，客户端保持透传；如果为空，客户端本地推导。

#### 15.2.6 保存后的缓存 patch 规则

基础档案保存成功：

```text
response.memberProfile
-> cacheContext.completeData.memberMedicalProfile = response.memberProfile
-> rebuildSections()
```

症状 mutation 成功：

```text
response.symptom 非空：
    upsert completeData.symptoms
response.deleted == true：
    remove completeData.symptoms[id]
response.memberProfile 非空：
    completeData.memberMedicalProfile = response.memberProfile
rebuildSections()
```

用药 mutation 成功：

```text
response.medicationPlan 非空：
    upsert completeData.medicationPlans
response.deleted == true：
    remove completeData.medicationPlans[id]
response.memberProfile 非空：
    completeData.memberMedicalProfile = response.memberProfile
rebuildSections()
```

手术 mutation 成功：

```text
response.surgery 非空：
    upsert completeData.surgeries
response.deleted == true：
    remove completeData.surgeries[id]
response.memberProfile 非空：
    completeData.memberMedicalProfile = response.memberProfile
rebuildSections()
```

模块设置保存成功：

```text
upsert completeData.memberModuleSettings by module_code
首页 shouldShowMedicalSection / shouldShowNutritionSection 重新计算
```

营养目标保存成功：

```text
cacheContext.nutritionGoalState = response
饮食模块汇总页刷新
医疗基础档案身高/体重仅作为初始值更新，不覆盖用户已经确认过的医疗基础档案草稿
```

#### 15.2.7 首页模块显示规则

首页模块展示只看模块配置：

```text
medical section:
    completeData.memberModuleSettings[module_code=medical].is_enabled == true

nutrition section:
    memberModuleSettings[module_code=nutrition].is_enabled == true
```

不要用以下数据推断是否展示首页模块：

```text
是否有症状
是否有用药
是否有 nutrition goals
是否有 memberMedicalProfile
```

原因：

```text
模块是否开启是用户配置；
模块内是否有数据是完成度。
```

#### 15.2.8 错误态与降级

医疗 completeData 失败：

```text
模块选择页医疗卡片展示“资料加载失败，可重试”
已开启状态如果本地有旧缓存，继续展示旧缓存
没有旧缓存时，允许用户进入医疗模块汇总页，页面内展示重试
```

nutrition goals 失败：

```text
如果 completeData 已返回 nutritionGoalState：不应出现该错误态
如果是补请求失败：不阻塞医疗模块，饮食健康卡片展示“目标资料未加载”
用户点击饮食健康时再重试 nutrition goals
```

memberModuleSettings 缺失：

```text
按空数组处理
模块默认未开启
用户完成模块维护并保存后再开启
```

memberMedicalProfile 缺失：

```text
医疗模块汇总页仍可通过 symptoms / medicationPlans / surgeries 推导部分摘要
基础档案、生活习惯、风险评估显示未填写
```

#### 15.2.9 日志要求

客户端关键日志：

```text
成员模块缓存：开始预加载 memberID=597
成员模块缓存：completeData 加载成功 memberID=597 symptoms=3 medicationPlans=4 surgeries=1 moduleSettings=2 hasProfile=1
成员模块缓存：completeData 内含 nutritionGoalState memberID=597 hasGoal=1
成员模块缓存：nutritionGoalState 补请求成功 memberID=597 hasGoal=1
成员模块缓存：nutritionGoalState 补请求失败 memberID=597 error=...
医疗模块汇总：命中缓存 memberID=597 sections=5
医疗模块汇总：缓存缺字段，补齐 completeData memberID=597 missing=memberMedicalProfile.guidanceSections
医疗模块汇总：保存后 patch 缓存 type=symptom id=10 deleted=0 hasProfile=1
```

服务端关键日志见 15.1.6。

### 15.3 测试与验收补充

#### 15.3.1 客户端单元测试建议

测试 `MemberMedicalModuleSummaryViewModel`：

```text
1. completeData 包含 guidanceSections 时，不触发网络请求。
2. completeData 不含 guidanceSections 但有 symptoms/medicationPlans/surgeries 时，可本地推导 sections。
3. symptom mutation 后，缓存 symptoms 更新且 profile 替换。
4. medication mutation 后，缓存 medicationPlans 更新且 profile 替换。
5. surgery mutation 后，缓存 surgeries 更新且 profile 替换。
6. module setting 更新后，memberModuleSettings 按 module_code upsert。
```

测试 `MemberModuleSetupViewModel`：

```text
1. 默认只加载 completeData，且从其中读取 nutritionGoalState。
2. 当 completeData 缺失 nutritionGoalState 时，才触发 nutrition goals 补请求。
3. nutrition goals 补请求失败不影响 medical 卡片展示。
4. 同 memberID 重复进入页面不会重复发起 in-flight 请求。
5. 切换 memberID 后重新加载 completeData，并按需补拉 nutrition goals。
```

#### 15.3.2 服务端测试建议

测试 `MemberCompleteDataAPI`：

```text
1. 返回 member_medical_profile。
2. 返回 member_module_settings，空数据时返回 []。
3. 不返回 medical_guidance。
4. 返回 nutrition_goal_state；缺失时允许为 null。
5. symptoms / medication_plans / surgeries 仍为完整 serializer payload。
6. 更新 profile 后 ETag 改变。
7. 更新 module settings 后 ETag 改变。
8. 更新 nutrition goals 后 ETag 改变。
```

#### 15.3.3 手工验收路径

路径一：首次进入选择维护模块页

```text
打开成员建档模块选择页
期望：
    complete-data 1 次
    默认不再额外请求 nutrition goals
    不请求 member-guidance
    不请求 member-module-settings
    不请求 symptoms / medication-plans / surgeries 独立列表
```

路径二：进入医疗模块汇总页

```text
从模块选择页点击医疗模块
期望：
    如果缓存完整，0 次请求
    页面直接展示基础档案、健康病史、生活习惯、体检档案、风险评估卡片
```

路径三：保存症状后返回汇总页

```text
新增症状
服务端返回 SymptomMutationResponse
客户端 patch completeData.symptoms + completeData.memberMedicalProfile
返回医疗汇总页不重新拉 symptoms 列表
```

路径四：保存营养目标

```text
进入饮食健康模块
保存目标
客户端更新 completeData.nutritionGoalState
服务端 complete-data 对应成员 ETag 变化
```

### 15.4 本工单不做的事

本工单不处理：

```text
completeData 轻量化摘要 DTO
member-guidance 接口立即删除
症状/用药/手术详情页 UI 重构
AI 风险评估算法重写
跨服务网关聚合
```

后续可单独建工单：

```text
MEMBER-000003 completeData 轻量化与分页策略
MEMBER-000004 member-guidance fallback 接口下线
MEMBER-000005 成员模块缓存 Store 抽象与跨页面共享
```

---

## 十六、最终结论

本工单建议支持并实施：

```text
进入选择维护模块时预加载 RemoteMemberCompleteData
RemoteMemberCompleteData 扩展 MemberMedicalProfile、module settings、nutritionGoalState
RemoteMemberMedicalProfile 承载医疗引导分组摘要、风险评估摘要、下一次体检计划摘要
医疗模块汇总页从缓存推导 sections
症状、用药、手术列表不再重复单独加载
提交成功后局部更新 completeData 缓存
```

最终目标：

```text
把一次进入医疗模块汇总页的 10 次请求压缩到 1-2 次；
让成员医疗模块汇总页成为缓存驱动页面；
让 RemoteMemberCompleteData 成为成员级医疗数据快照；
避免 ViewModel 之间各自加载同一批成员资料。
```
