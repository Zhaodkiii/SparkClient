# 用药通知需求讨论文档

> 文档性质：需求讨论文档。本文只分析 SparkClient / SparkService 当前项目在增加“用药通知”时的方案选择、影响范围、风险点、待确认问题和建议路线，不直接修改代码。

## 工单索引

| 工单号 | 工单名 | 状态 | 范围 |
| --- | --- | --- | --- |
| `MEDICATION-NOTIFICATION-000001` | 用药通知方案选型与系统设计讨论 | 需求讨论中 | 分析服务端 APNs 推送、客户端本地通知、混合方案在用药提醒场景下的优缺点；补充权限、时区、跨设备、计划变更、打卡反馈、重复通知、隐私与容错设计 |

## 工单 `MEDICATION-NOTIFICATION-000001`：用药通知方案选型与系统设计讨论

### 工单状态

需求讨论中。

## 1. 当前项目现状

### 1.1 SparkClient 已有能力

| 能力 | 当前代码位置 | 当前情况 |
| --- | --- | --- |
| 用药计划字段 | `SparkClient/SparkClient/Projects/Core/Networking/API/Medical/MedicalSyncAPI.swift` | `RemoteMedicationPlan` 已包含 `frequencyType`、`everyNDays`、`weeklyWeekdays`、`reminderTimes`、`startDate`、`endDate`、`reminderEnabled` |
| 用药记录字段 | `SparkClient/SparkClient/Projects/Core/Networking/API/Medical/MedicalSyncAPI.swift` | `RemoteMedicationRecord` 已包含 `scheduledAt`、`takenAt`、`status`、`doseSequence`、`timezone` |
| 用药计划表单 | `SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationsListPage.swift` | 表单已支持“开启提醒”、提醒时间、多种频次和起止日期 |
| 本地通知能力 | `SparkClient/SparkClient/Projects/Core/Notification/Infrastructure/TaskNotificationManager.swift` | 已有基于 `UNCalendarNotificationTrigger` 的任务本地通知注册、更新、移除能力，但当前是 `HealthTask` 任务提醒，不是用药计划提醒 |
| APNs 推送能力 | `SparkClient/SparkClient/Projects/Core/Notification/Infrastructure/PushAdapter.swift` | 已有请求通知权限、注册 APNs token、前台接收、点击接收能力 |
| 设备推送登记 | `SparkClient/SparkClient/Projects/Core/Device/Application/DeviceRegistrationCoordinator.swift` | 已有通知权限状态、APNs token、设备画像聚合上送能力 |
| 通知路由 | `SparkClient/SparkClient/Projects/Core/Notification/Application/HandleRemoteNotificationUseCase.swift` | 已支持按 `type` 处理远程通知，并可触发应用内 banner、toast、路由 |

### 1.2 SparkService 已有能力

| 能力 | 当前代码位置 | 当前情况 |
| --- | --- | --- |
| 用药计划模型 | `SparkService/medical/models.py` | `MedicationPlan` 已有提醒相关字段，但它本身不是通知调度表 |
| 用药记录模型 | `SparkService/medical/models.py` | `MedicationRecord` 可记录某次计划剂次状态 |
| 任务通知模型 | `SparkService/task_system/models.py` | `TaskNotification` 已有 `channel`、`status`、`reminder_time`、`template_params` |
| 服务端调度雏形 | `SparkService/task_system/notification_tasks.py` | 当前 Celery 任务默认只做状态落库与日志，注释说明后续可对接 APNs/SMS |
| 创建任务时预置提醒 | `SparkService/task_system/views.py` | 创建 `Task` 后会创建默认 `TaskNotification`，并可按 `eta` 调度一次提醒 |

### 1.3 HealthClient 参考方案

参考路径：

```text
/Users/hua/Downloads/Reference/包/Health/HealthClient
```

其中任务引擎设计文档给出的方向是：

1. 本地通知：无网可用，用于当日提醒。
2. 通知注册表：需要换机、跨端、App 被杀后仍可靠提醒时，向服务端登记。
3. 服务端只做通知中转，不理解医学语义，不负责“该不该吃药”的业务判断。
4. 通知 payload 尽量不明文携带病情和药品详情，点击进入 App 后再加载详情。

这个方向对 SparkClient 很有参考价值：用药提醒的医学规则、时间规则和用户体验最好由客户端可解释地生成；服务端更适合作为跨设备同步和兜底推送通道。

## 2. 方案一：服务端触发 iOS APNs 推送通知

### 2.1 实现方式

服务端根据 `MedicationPlan` 生成待提醒任务，在提醒时间到达后通过 APNs 推送到用户设备。

典型流程：

```text
用户创建/编辑用药计划
  -> 客户端保存 MedicationPlan 到服务端
  -> 服务端根据 frequencyType / reminderTimes / startDate / endDate 生成提醒计划
  -> 服务端调度任务到点触发
  -> 服务端通过 APNs 推送到设备
  -> 用户点击通知进入 App
  -> App 根据 payload 中的 member_id / plan_id / scheduled_at 加载用药详情
```

### 2.2 优点

| 优点 | 说明 |
| --- | --- |
| 跨设备一致 | 同一个账号多台设备登录时，由服务端统一调度，不需要每台设备分别推导提醒 |
| 服务端可审计 | 可以记录通知是否生成、是否发送、失败原因、推送设备 |
| 对换机友好 | 新设备只要登录并上报 APNs token，服务端可继续提醒 |
| 对后台/卸载边界更可控 | App 长期不打开时，服务端仍能尝试推送 |
| 便于运营扩展 | 后续可扩展短信、家属代提醒、家庭成员共享提醒等 |
| 与现有 APNs 基础设施一致 | SparkClient 已有 `PushAdapter`、设备登记、通知路由，SparkService 也已有任务通知雏形 |

### 2.3 缺点

| 缺点 | 说明 |
| --- | --- |
| 强依赖网络和 APNs | 用户离线、APNs token 失效、系统通知关闭时无法保证到达 |
| 服务端复杂度高 | 服务端需要理解用药计划频次、起止日期、时区、跳过、暂停、修改、删除、补服等规则 |
| 高频调度压力 | 用药提醒是日常高频通知，多用户多药品会带来大量定时任务或扫表任务 |
| 计划变更一致性难 | 用户编辑计划后，旧调度任务必须取消或失效，否则容易重复提醒 |
| 时区和夏令时风险 | 服务端若按 UTC 生成未来多天提醒，用户旅行或系统时区变化后容易错点 |
| 前台体验不一定好 | App 正在使用时，系统 banner 通常不是最佳体验，需要转应用内提醒 |
| 隐私风险更高 | APNs payload 如果带药品名、病情、剂量，会在锁屏暴露敏感健康信息 |
| iOS 不保证准时送达 | 远程推送不是强实时机制，尤其省电、弱网、系统策略下可能延迟 |

### 2.4 适合场景

1. 账号多设备共用，希望所有设备都能收到提醒。
2. 用户长期不打开 App，但仍希望服务器兜底提醒。
3. 需要家属、照护人、共享成员收到提醒。
4. 后台需要通知记录、触达统计、失败重试。
5. 未来要支持 Web、Android、iOS 统一提醒中心。

### 2.5 不适合单独承担的原因

用药提醒属于“按本地时间准点提醒”的强体验场景，只靠服务端 APNs 会有三个硬伤：

1. iOS 不承诺远程推送准点到达。
2. 没有网络时无法触发新通知。
3. 用户临时调整时区、当天已服药、临时跳过后，服务端和客户端状态可能短时间不一致。

所以 APNs 更适合作为“跨端/兜底/共享提醒”，不建议作为首版唯一方案。

## 3. 方案二：客户端应用内创建本地通知

### 3.1 实现方式

客户端在创建或更新 `MedicationPlan` 后，根据计划规则生成本地 `UNNotificationRequest`。通知到点由 iOS 系统本地触发，不依赖服务端实时调度。

典型流程：

```text
用户创建/编辑用药计划
  -> 客户端保存 MedicationPlan 到服务端
  -> 保存成功后客户端本地编译提醒计划
  -> 注册未来一段时间的 UNNotificationRequest
  -> 用户收到本地通知
  -> 点击通知进入 App
  -> App 根据本地 payload 中的 plan_id / scheduled_at 打开用药执行中心或计划详情
```

### 3.2 优点

| 优点 | 说明 |
| --- | --- |
| 准时性更适合用药 | 本地日历触发比 APNs 更适合“每天 08:00 吃药” |
| 离线可用 | 没网络也能提醒 |
| 服务端压力小 | 不需要服务端为每个剂次调度大量任务 |
| 隐私更可控 | 可以只在本地生成通知内容，服务端不需要持有每次提醒 payload |
| 响应计划变更快 | 用户编辑、暂停、删除计划后，本机可立即取消和重建通知 |
| 符合 HealthClient 参考方向 | HealthClient 文档也将本地通知作为当日提醒主路径 |
| 可先低成本落地 | SparkClient 已有 `TaskNotificationManager` 可参考拆出通用本地通知注册能力 |

### 3.3 缺点

| 缺点 | 说明 |
| --- | --- |
| 单设备生效 | 用户换手机或另一台设备登录后，需要重新同步计划并注册本地通知 |
| App 被删除后不可用 | 本地通知随 App 删除消失 |
| iOS 待通知数量有限 | iOS 对 pending local notifications 有数量限制，不能无限注册未来所有剂次 |
| 需要本地编译器 | 客户端需要稳定实现 daily / every_n_days / weekly 等频次展开 |
| 多设备可能重复 | 同一账号多设备都注册本地通知时，用户可能多台设备同时响 |
| 权限状态仍需处理 | 本地通知也依赖系统通知权限，用户拒绝后无法弹系统提醒 |
| 数据同步后要重建 | 服务端返回计划变更、删除、停用后，本地必须及时重建通知 |

### 3.4 适合场景

1. 单设备主要使用。
2. 需要准时、离线、低延迟提醒。
3. 首版希望快速形成闭环。
4. 通知内容希望尽量留在本地生成。
5. 用药计划变动频繁，需要本机快速取消旧提醒。

### 3.5 不适合单独承担的原因

只做本地通知会在跨端场景下变弱：

1. 换机后未打开 App 或未完成同步，就不会注册提醒。
2. 家属照护、共享成员提醒无法自然覆盖。
3. 服务端没有通知登记状态，排查“为什么没提醒”会比较困难。

所以本地通知适合做“主提醒”，但长期最好补一层服务端通知登记和状态可观测。

## 4. 推荐方案：本地通知为主，服务端 APNs 作为跨端与兜底

### 4.1 推荐结论

首版推荐采用混合架构：

```text
客户端本地通知 = 主提醒通道
服务端 APNs = 跨设备/换机/共享/兜底通道
服务端不负责医学判断，只保存通知登记和调度元数据
```

理由：

1. 用药提醒最核心的是准时和离线，本地通知更合适。
2. SparkClient 已经有本地通知和 APNs 两套基础能力，适合做分层复用。
3. SparkService 的 `task_system` 已有通知表雏形，但 APNs 发送还未真正接入，不适合首版把所有提醒都压到服务端。
4. 用药计划规则在客户端表单里已经完整表达，客户端可以直接编译提醒。
5. 服务端保留登记能力，未来可以扩展多设备、家属提醒、失败排查。

### 4.2 分阶段落地建议

#### 阶段一：只做客户端本地通知闭环

目标：

1. 创建/编辑/删除/停用用药计划后，本机通知立即同步。
2. 用药提醒能在无网情况下触发。
3. 点击通知进入用药执行中心，并定位到对应成员、计划、剂次时间。
4. 服药打卡后，当前剂次的提醒不再重复出现。

不做：

1. 不新增服务端调度。
2. 不做家属代提醒。
3. 不做跨设备统一去重。
4. 不做长期未来所有通知一次性注册。

#### 阶段二：增加服务端通知登记表

目标：

1. 客户端把本地编译后的提醒摘要登记到服务端。
2. 服务端记录 `plan_id`、`member_id`、`next_fire_at`、`timezone`、`schedule_fingerprint`、`device_id`、`status`。
3. 服务端可用于换机后恢复、设备诊断、后续 APNs 兜底。
4. 服务端仍不展开复杂医学规则，不判断“应该吃什么药”。

#### 阶段三：增加 APNs 兜底提醒

目标：

1. 当用户开启“跨设备/家属提醒/服务端兜底”时，服务端按登记表触发 APNs。
2. APNs payload 不暴露敏感药品详情，只携带路由和资源 ID。
3. 前台收到远程通知时转应用内 banner，不弹系统 banner。
4. 点击后刷新用药计划和当天用药记录，避免展示过期状态。

## 5. 用药通知需要考虑的核心问题

### 5.1 提醒时间规则

需要明确支持哪些规则：

| 规则 | 当前字段支持 | 需要注意 |
| --- | --- | --- |
| 每天 | `frequencyType = daily` | 按 `reminderTimes` 每天生成剂次 |
| 每几天 | `frequencyType = every_n_days` + `everyNDays` | 必须以 `startDate` 作为周期锚点 |
| 每周指定星期 | `frequencyType = weekly` + `weeklyWeekdays` | 需要统一 1=周一...7=周日 |
| 多个时间点 | `reminderTimes` | 同一天多个剂次要有稳定 `doseSequence` |
| 开始/结束日期 | `startDate` / `endDate` | 结束日期当天是否包含，需要明确为“包含结束日” |
| 暂停/停用 | `status` / `reminderEnabled` | 关闭后必须取消本地 pending 通知 |

### 5.2 本地通知注册窗口

不建议一次性注册整个用药周期的所有通知，因为 iOS pending notification 数量有限。

建议：

1. 每次只注册未来 7 天或 14 天的用药提醒。
2. App 冷启动、进入前台、用药计划变更后重新滚动刷新。
3. 每个通知 request identifier 必须可重复计算，便于取消和覆盖。

建议 identifier：

```text
medication_plan_{planID}_{yyyyMMdd_HHmm}_{doseSequence}
```

如果同一计划同一时间可能有多剂量，增加 `doseSequence` 或 `reminderTimeIndex`。

### 5.3 点击通知后的路由

建议本地和远程通知 payload 统一路由字段：

```json
{
  "type": "medication_reminder",
  "route": "medication_execution",
  "member_id": 100,
  "plan_id": 12,
  "scheduled_at": "2026-06-15T08:00:00+08:00",
  "dose_sequence": 1
}
```

点击目标建议：

1. 优先进入 `MedicationExecutionCenterPage`。
2. 自动切到 `scheduled_at` 对应日期。
3. 如果能定位到计划剂次，高亮待服药卡片。
4. 如果计划已删除、停用、成员无权限，展示“该提醒已失效”。

### 5.4 通知内容隐私

用药通知属于敏感健康信息，锁屏文案要谨慎。

可选文案策略：

| 策略 | 示例 | 优点 | 缺点 |
| --- | --- | --- | --- |
| 隐私优先 | 该记录一项用药了 | 锁屏不暴露药名 | 用户不知道吃哪种 |
| 平衡策略 | 妈妈有一项用药提醒 | 暴露较少，能区分成员 | 不显示药名 |
| 明确策略 | 妈妈该服用阿莫西林了 | 行动明确 | 锁屏暴露药品 |

建议首版默认使用平衡策略，并在设置里允许用户选择是否显示药品名称。

### 5.5 权限请求时机

不建议在 App 启动时直接弹通知权限。

推荐触发时机：

1. 用户第一次开启某个用药计划的提醒。
2. 用户保存带 `reminderEnabled = true` 的用药计划。
3. 用户进入“用药通知设置”并主动开启。

如果权限未决定：

1. 先弹应用内说明页，说明“我们会在用药时间提醒你”。
2. 用户确认后调用系统通知权限。

如果权限被拒绝：

1. 提示去系统设置开启。
2. 用药计划仍可保存，但提醒状态展示“系统通知未开启”。
3. 本地保存通知配置，权限恢复后自动注册。

### 5.6 用药记录与通知取消

用户完成或跳过某次用药后，需要处理后续通知：

1. 当前剂次未触发的 pending 通知应取消。
2. 已触发但仍在通知中心的 delivered 通知应移除。
3. 同一计划当天其他剂次不受影响。
4. 如果用户撤销打卡，需要可重新注册未来对应剂次。

### 5.7 多设备重复提醒

混合方案必须处理多设备：

| 场景 | 建议 |
| --- | --- |
| 只有本地通知 | 每台已登录设备都可注册提醒，可能重复，但实现简单 |
| 只选主设备提醒 | 需要服务端记录 active reminder device，复杂度更高 |
| APNs 兜底 | 可以只推最近活跃设备，或推所有有权限设备 |

首版建议：

1. 本地通知默认在当前设备生效。
2. 多设备重复提醒先作为已知限制。
3. 服务端登记时记录 `device_id`，后续再做“仅主设备提醒”。

### 5.8 时区与旅行

用药计划通常应按用户当前本地时间提醒，例如每天 08:00。

建议：

1. 本地通知使用 `UNCalendarNotificationTrigger` 的本地 calendar 语义。
2. 计划保存时保留 `timezone` 或在通知登记中保留 `timezone_identifier`。
3. App 发现系统时区变化后，重建未来通知窗口。
4. 服务端 APNs 兜底需要根据最近设备时区计算 `next_fire_at`，不能永久使用创建计划时的时区。

### 5.9 勿扰与安静时段

需要确认是否做应用内勿扰：

1. 系统勿扰由 iOS 控制，App 不需要绕过。
2. 应用内可设置“安静时段”，例如 22:00-07:00 不提醒或延后提醒。
3. 医疗用药是否允许延后需要谨慎，首版建议只提示，不自动改药。

### 5.10 计划变更一致性

以下操作必须重建通知：

1. 新增用药计划。
2. 编辑药名、剂量、频次、提醒时间、开始日期、结束日期。
3. 关闭 `reminderEnabled`。
4. 停用或删除计划。
5. 成员解绑或权限失效。
6. 服务端同步到远端变更。
7. 登录账号切换。
8. App 时区变化。

### 5.11 通知状态可观测

为了排查“为什么没提醒”，建议在客户端保留诊断信息：

1. 最近一次通知重建时间。
2. 当前成员/计划注册了多少条 pending 通知。
3. 系统通知权限状态。
4. 最近一次注册失败原因。
5. 服务端 APNs token 是否存在。

这些信息可以先只做 Debug 日志，后续再进入设置页诊断。

## 6. 建议新增模块设计

### 6.1 客户端模块

建议不要把用药通知逻辑直接写进页面 View。可以拆成：

| 模块 | 职责 |
| --- | --- |
| `MedicationReminderScheduleCompiler` | 把 `RemoteMedicationPlan` 编译为未来 N 天的提醒实例 |
| `MedicationReminderNotificationManager` | 注册、更新、取消本地 `UNNotificationRequest` |
| `MedicationReminderPermissionCoordinator` | 处理权限请求、设置跳转、权限状态 |
| `MedicationReminderRouteHandler` | 处理通知点击后进入用药执行中心 |
| `MedicationReminderSyncCoordinator` | 监听计划保存、删除、同步、账号切换、时区变化并触发重建 |

### 6.2 服务端模块

如果进入阶段二/三，服务端建议不要复用 `TaskNotification` 直接承载全部用药提醒语义。更合适的是先抽象一张“通知登记表”或“用药提醒登记表”：

| 字段 | 说明 |
| --- | --- |
| `user` | 账号 |
| `member` | 家庭成员 |
| `plan_id` | 用药计划 ID |
| `device_id` | 登记设备 |
| `schedule_fingerprint` | 客户端编译规则哈希 |
| `next_fire_at` | 下次服务端兜底触发时间 |
| `timezone_identifier` | 时区 |
| `status` | active / paused / cancelled / failed |
| `last_sent_at` | 最近发送时间 |
| `last_error` | 最近失败原因 |

原因：

1. `TaskNotification` 当前是任务系统模型，语义偏“任务提醒”。
2. 用药提醒是高频、周期性、可由本地编译的医疗计划提醒，生命周期和任务不完全一样。
3. 如果强行把用药提醒塞进任务系统，后续容易出现任务域和用药域职责混杂。

不过第一期可以不新增服务端表，只做客户端本地闭环。

## 7. 推荐交互设计

### 7.1 创建/编辑用药计划

在用药计划表单中保留现有“开启提醒”开关。

保存时：

1. 如果 `reminderEnabled = false`：只保存计划，不注册通知。
2. 如果 `reminderEnabled = true` 且系统通知已授权：保存成功后注册本地通知。
3. 如果 `reminderEnabled = true` 且系统通知未决定：展示应用内说明，再请求系统权限。
4. 如果 `reminderEnabled = true` 且系统通知被拒绝：保存计划，但提示用户去设置开启通知。

### 7.2 用药执行中心

建议增加轻量提示：

1. 如果今天有开启提醒的计划，但系统通知关闭，展示“通知未开启，可能错过用药提醒”。
2. 如果计划已开启提醒，卡片上显示提醒时间和通知状态。
3. 用户完成服药后，自动清除当前剂次通知。

### 7.3 设置入口

可以在用药模块增加“用药通知设置”：

1. 系统通知权限状态。
2. 是否在通知中显示药品名称。
3. 默认提前提醒时间，首版可以不做。
4. 多设备提醒策略，首版可以不做。
5. 通知诊断，Debug 或后续版本再做。

## 8. 风险与注意点

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| 重复通知 | 编辑计划后旧通知未取消 | 使用稳定 identifier，更新前先批量移除旧计划通知 |
| 漏通知 | App 同步到新计划但未重建本地通知 | 在同步完成、前台恢复、账号切换后触发重建 |
| 通知数量超限 | 长期计划每天多次提醒 | 只注册未来 7/14 天滚动窗口 |
| 敏感信息暴露 | 锁屏显示药名、病情 | 默认不显示药名，用户可选择 |
| 时区错乱 | 用户旅行后提醒时间异常 | 检测时区变化后重建 |
| 多设备打扰 | 多台设备同时提醒 | 首版记录已知限制，后续服务端主设备策略 |
| 服务端过度设计 | 一开始做完整调度系统 | 首版先本地通知，服务端登记放二期 |
| 业务职责泄漏 | 页面直接计算提醒规则 | 抽出 schedule compiler 和 notification manager |
| APNs 到达不稳定 | 远程推送延迟 | 只作为兜底，不作为主提醒 |
| 医疗安全 | AI 或规则自动改提醒 | 不自动改变医嘱时间，用户确认后保存 |

## 9. 影响文件预估

### 9.1 客户端

```text
SparkClient/SparkClient/Projects/Core/Networking/API/Medical/MedicalSyncAPI.swift
SparkClient/SparkClient/Projects/Core/Notification/Infrastructure/TaskNotificationManager.swift
SparkClient/SparkClient/Projects/Core/Notification/Infrastructure/PushAdapter.swift
SparkClient/SparkClient/Projects/Core/Notification/Application/HandleRemoteNotificationUseCase.swift
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationsListPage.swift
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationExecutionCenter/MedicationExecutionCenterPage.swift
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationExecutionCenter/MedicationExecutionModels.swift
```

建议新增：

```text
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationNotification/
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationNotification/MedicationReminderScheduleCompiler.swift
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationNotification/MedicationReminderNotificationManager.swift
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationNotification/MedicationReminderPermissionCoordinator.swift
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationNotification/MedicationReminderRouteHandler.swift
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Medications/MedicationNotification/MedicationReminderSyncCoordinator.swift
```

### 9.2 服务端

阶段一不需要改服务端。

阶段二/三可能涉及：

```text
SparkService/medical/models.py
SparkService/medical/serializers.py
SparkService/medical/views.py
SparkService/task_system/models.py
SparkService/task_system/notification_tasks.py
SparkService/accounts/services/notification_service.py
```

如果复用公共 APNs 通知，应优先复用现有 accounts 通知服务，不要在 medical 内重复实现 APNs 发送。

## 10. 待确认问题

### Q1：首版是否只做本地通知？

建议：首版只做客户端本地通知闭环，服务端 APNs 放二期。这样能最快解决“准时提醒”和“离线可用”。

待确认：

```text
实现方向：
备注：
```

### Q2：通知内容是否显示药品名称？

建议：默认不显示药品名称，只显示“成员 + 用药提醒”。设置中允许用户开启显示药品名称。

待确认：

```text
实现方向：
备注：
```

### Q3：本地通知一次注册未来几天？

建议：首版注册未来 7 天；前台恢复、冷启动、计划变更后滚动重建。

待确认：

```text
实现方向：
备注：
```

### Q4：多设备是否允许重复提醒？

建议：首版允许当前设备本地提醒，不做主设备去重；二期通过服务端登记解决。

待确认：

```text
实现方向：
备注：
```

### Q5：点击通知进入哪里？

建议：进入用药执行中心，定位到提醒日期，并高亮对应计划剂次；失效时展示不可用提示。

待确认：

```text
实现方向：
备注：
```

### Q6：服药打卡后是否清除通知中心已展示通知？

建议：清除同一 `planID + scheduledAt + doseSequence` 的 pending 和 delivered 通知，避免用户打卡后还看到旧提醒。

待确认：

```text
实现方向：
备注：
```

### Q7：服务端 APNs 是否作为二期兜底？

建议：二期做通知登记表，三期再接入 APNs 兜底；不要第一期直接做完整服务端调度。

待确认：

```text
实现方向：
备注：
```

## 11. 验收标准草案

### 11.1 阶段一验收

1. 保存开启提醒的用药计划后，本机能在指定时间收到通知。
2. 关闭提醒后，该计划未来通知全部取消。
3. 编辑提醒时间后，旧时间不再提醒，新时间正常提醒。
4. 删除或停用计划后，不再收到该计划通知。
5. App 无网时，本地通知仍能触发。
6. 通知权限未开启时，保存计划不失败，但页面明确提示提醒不可用。
7. 点击通知进入用药执行中心，并定位到对应日期。
8. 完成或跳过某次用药后，该剂次通知不再重复出现。
9. 账号切换后，不展示上一个账号的用药通知。
10. App 前台恢复后能重建未来提醒窗口。

### 11.2 阶段二/三验收

1. 客户端可把提醒登记同步到服务端。
2. 服务端可以看到每个计划的下次提醒时间和登记设备。
3. APNs payload 不包含敏感药品详情。
4. 远程通知点击后能进入正确页面并刷新用药数据。
5. 设备 token 失效或通知关闭时，服务端记录失败但不影响用药计划本身。
6. 多设备策略明确，不出现不可解释的大量重复提醒。

