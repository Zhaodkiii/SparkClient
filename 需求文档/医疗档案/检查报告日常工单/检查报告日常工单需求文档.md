# 检查报告日常工单需求文档

> 文档性质：需求文档。本文用于记录 `ExaminationReportsListPage` 相关日常需求、入口改造、交互要求和验收标准。本文仅描述需求与实现范围，不直接修改代码。

## 工单索引

| 工单号 | 工单名 | 状态 | 范围 |
| --- | --- | --- | --- |
| `EXAMINATION-REPORT-000001` | 检查/体检报告拍摄自定义相机 | 需求设计中 | 在检查报告、体检报告上传入口中新增医疗报告拍摄自定义相机，替换当前系统相机拍照路径；参考药箱自定义相机的优秀交互，按检查报告/体检报告的纸张与 OCR 场景重新设计取景比例、引导、底部横向滚动预览、预览删除和返回多图能力 |
| `EXAMINATION-REPORT-000002` | 医疗检查报告列表页 UI 结构还原 | 需求记录中 | 根据现有 `ExaminationReportsListPage` 与截图记录医疗检查报告列表页的 plain text UI 布局、卡片结构、分类展示、底部操作栏和验收要点 |
| `EXAMINATION-REPORT-000003` | 检查报告列表时间优先排序与连续分类标题合并 | 需求记录中 | 将检查报告列表从固定大类分组展示改为先按报告时间排序；滚动流中只在连续同类卡片段落开始处展示一次分类标题，避免同一连续日期/同一连续分类重复出现标题 |

## 工单 `EXAMINATION-REPORT-000001`：检查/体检报告拍摄自定义相机

### 工单状态

需求设计中。

## 1. 背景

检查报告列表页当前已经提供“拍摄添加报告”入口：

```text
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/ExaminationReports/ExaminationReportsListPage.swift:143-147
```

该入口打开通用医疗附件上传列表：

```swift
MedicalAttachmentUploadListSheet(documentType: .examinationReport) { files in
    startExaminationReportRecognition(files: files)
}
```

通用上传列表中，检查报告当前仍使用系统相机：

```text
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Shared/MedicalAttachmentUploadListSheet.swift:117-122
```

现有策略：

```swift
case .medicineBox:
    return .medicineBoxCustomCamera
case .medicationPlan, .examinationReport, .healthExamReport, .caseDocument:
    return .systemCamera
```

因此用户在检查报告、体检报告上传 Sheet 中点击“拍照”时，仍打开 `SystemImagePicker(source: .camera)`。系统相机无法提供报告拍摄所需的取景框、纸张边界提示、连续拍摄、拍后横向预览管理和 OCR 友好提示。

本工单要求在 `CustomCamera/Presentation/Scene` 内新增“医疗报告拍摄自定义相机”，并把检查报告、体检报告上传入口的拍照路径切换到该自定义相机。

## 2. 目标

### 2.1 核心目标

1. 在 `CustomCamera/Presentation/Scene` 下新增医疗报告拍摄自定义相机场景。
2. 检查报告、体检报告上传 Sheet 点击“拍照”时，不再打开系统相机，改为打开医疗报告自定义相机。
3. 自定义相机复用现有 `CustomCameraView`、`CustomCameraManager`、`DefaultCustomCapturedMediaScreen` 等底层能力。
4. 参考 `MedicineBoxCameraSceneView` 的优秀设计，包括首次引导、连续拍摄、拍后缩略图、删除、QuickLook 预览、完成返回多图。
5. 不直接照搬药盒取景比例，应按医疗报告/检查报告/体检报告常见纸张形态重新设计取景框比例。
6. 拍摄结果继续转成 `[MedicalUploadLocalFile]`，保持后续 `startExaminationReportRecognition(files:)` 识别流程不变。
7. 保留相册选择、文件导入、文件预览、最大数量限制等现有上传列表能力。
8. 检查报告入口最多拍摄 3 张图片；体检报告入口最多拍摄 6 张图片。

### 2.2 非目标

1. 本工单不修改服务端识别接口。
2. 本工单不改变 `MedicalUploadLocalFile` 数据结构。
3. 本工单不实现自动纸张边缘检测、自动裁切、透视矫正、OCR 预处理算法。
4. 本工单不改变病例、服药计划等其他上传入口的相机策略。
5. 本工单不改检查报告、体检报告识别结果页和保存入库逻辑。

## 3. 参考实现分析

### 3.1 MedicineBox 值得复用的设计

参考文件：

```text
SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/MedicineBox/MedicineBoxCameraSceneView.swift
SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/MedicineBox/MedicineBoxCameraGuideView.swift
SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/MedicineBox/MedicineBoxCaptureSlot.swift
SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/MedicineBox/MedicineBoxCameraViewfinderShape.swift
```

可直接复用或仿照的优秀点：

1. `fullScreenCover` 打开自定义相机，退出后回到上传列表。
2. 使用 `CustomCameraView(manager:)` 组装底层相机，不再依赖系统 `UIImagePickerController`。
3. 首次进入展示拍摄引导，并通过 `GuideStore` 记录是否已读。
4. 拍摄后先进入默认确认页，用户确认后才追加到底部横向预览列表。
5. 支持连续拍摄，确认后将图片追加到底部横向滚动预览列表。
6. 底部预览列表展示缩略图，用户能明确知道已经拍了哪些内容。
7. 已拍图片支持删除。
8. 点击已拍图片时打开 QuickLook 预览，预览删除规则沿用用药相机的一致交互。
9. 点击完成时按拍摄顺序返回图片，不设置必拍项。
10. 返回给上层的是业务结构数组，上层再负责保存临时文件和发起识别。

### 3.2 MedicineBox 不适合直接照搬的问题

药盒相机是为药品包装、保质期、说明书设计的，不适合原样用于检查报告：

1. 取景框比例偏向药盒/说明书混合场景，当前实现高度最大约 `viewfinderWidth * 1.18`，不完全匹配 A4 竖版报告的 `1:1.414` 比例，也不匹配横版影像报告或长条检验单。
2. 底部面板高度按屏幕高度 `26%` 且最低 `220pt`，再用 `panelHeight / 268` 线性缩放内部控件。小屏上底部面板会挤压取景区，大屏上控件可能随面板变大而显得过重。
3. 取景框尺寸主要受屏幕宽度驱动，报告拍摄更应该同时考虑纸张比例、OCR 文字可读性、上下安全区和底部预览列表占用。
4. 药盒四角取景框只提示“把目标放进框内”，缺少报告拍摄所需的纸张边缘、留白、平整、避免反光等提示。
5. 药盒槽位是固定业务内容：药盒正面、保质期、说明书；检查报告和体检报告页数不固定，更适合按拍摄顺序追加到底部横向滚动预览列表。
6. 药盒需求文档与当前代码存在必拍项描述不一致风险：文档曾描述“药盒正面 + 说明书”必拍，但当前代码 `canFinish` 校验的是 `.front` 和 `.expiry`。医疗报告相机本工单取消必拍项，避免相机侧与业务识别侧产生额外校验分歧。
7. 药盒底部左侧显示固定业务标题和图标，医疗报告拍摄应更关注页数、清晰度和 OCR 提示，避免占用过多底部空间。

## 4. 新增相机场景设计

### 4.1 目录与命名

新增目录：

```text
SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/ExaminationReport/
```

建议新增文件：

| 文件 | 职责 |
| --- | --- |
| `ExaminationReportCameraSceneView.swift` | 医疗报告自定义相机场景入口，维护拍摄状态、引导、横向预览列表、完成返回 |
| `ExaminationReportCameraGuideView.swift` | 首次拍摄引导页，说明报告拍摄要求 |
| `ExaminationReportCameraGuideStore.swift` | 记录医疗报告相机引导是否已展示 |
| `ExaminationReportCapturedImage.swift` | 医疗报告已拍图片模型，记录拍摄顺序、图片和文件名后缀 |
| `ExaminationReportCameraViewfinderShape.swift` | 医疗报告取景框形状，可复用四角框基础实现但按报告比例增强 |

### 4.2 对外接口

建议接口：

```swift
struct ExaminationReportCameraSceneView: View {
    let onCancel: () -> Void
    let onImagesCaptured: ([ExaminationReportCapturedImage]) -> Void
    let maxCaptureCount: Int
}
```

返回结构：

```swift
struct ExaminationReportCapturedImage: Identifiable, Equatable {
    let id: UUID
    let index: Int
    let image: UIImage
}
```

上层上传列表继续负责：

1. 将 `UIImage` 保存为临时 jpg。
2. 文件名前缀使用 `examination_report_camera_` 或 `health_exam_report_camera_`。
3. 追加到 `localFiles`。
4. 点击确认后，检查报告调用 `startExaminationReportRecognition(files:)`，体检报告调用既有体检报告识别流程。

## 5. 拍摄预览设计

### 5.1 横向滚动预览方案

取消固定槽位，不再预设 `报告首页`、`明细页`、`补充页`。用户每完成一次拍摄确认，图片按拍摄顺序追加到底部横向滚动预览列表。

```text
[第 1 张] [第 2 张] [第 3 张] ...
```

不同入口的最大拍摄数量：

| 入口 | 最大拍摄数量 | 说明 |
| --- | --- | --- |
| 检查报告 | 3 张 | 适合单页或少量附页的检查、检验、影像报告 |
| 体检报告 | 6 张 | 体检报告通常页数更多，允许拍摄更多页面 |

说明：

1. 不设置必拍项，用户可以在未拍摄图片时返回取消，也可以在至少拍摄 1 张后点击完成。
2. 图片顺序即拍摄顺序，返回给上传列表时不再按预设业务位置重排。
3. 缩略图显示页码序号，例如 `1`、`2`、`3`。
4. 到达最大拍摄数量后，拍摄按钮置灰或点击提示“已达到本次拍摄上限”。
5. 如果用户需要上传更多图片，可回到上传列表继续通过相册或文件导入补充。

### 5.2 完成规则

1. 不校验必拍项。
2. 已拍摄至少 1 张图片时，点击完成关闭相机并返回已拍图片。
3. 未拍摄任何图片时，完成按钮置灰；如仍允许点击，应提示“请先拍摄报告图片”。
4. 返回顺序固定为用户拍摄确认的顺序。
5. 删除中间图片后，剩余图片重新按当前顺序展示序号。

## 6. 取景框比例与布局

### 6.1 设计原则

检查报告相机的取景框应服务 OCR 和人工拍摄稳定性：

1. 以竖版报告为默认，优先适配 A4/医院报告单。
2. 取景框宽高比建议接近 `1:1.414`，但需要在小屏上根据可用高度压缩到不遮挡底部控件。
3. 允许横向报告、影像胶片拍摄时使用同一大框容纳，不做第一版横竖切换按钮。
4. 取景框内增加弱提示线或边缘留白提示，提醒用户让报告四边完整入框。
5. 不使用纯固定高度或单一 `width * 1.18` 规则。

### 6.2 推荐布局计算

建议将布局计算从药盒的“底部面板比例驱动”改为“可用拍摄区域 + 报告目标比例”驱动：

```swift
let availableWidth = proxy.size.width
let availableHeight = proxy.size.height
let safeTop = proxy.safeAreaInsets.top
let safeBottom = proxy.safeAreaInsets.bottom

let bottomPanelHeight = min(max(196, availableHeight * 0.24), 252)
let topToolbarHeight = safeTop + 58
let promptHeight: CGFloat = 48
let verticalGap: CGFloat = 12

let cameraAreaTop = topToolbarHeight + verticalGap
let cameraAreaBottom = availableHeight - safeBottom - bottomPanelHeight - promptHeight - verticalGap
let cameraAreaHeight = max(260, cameraAreaBottom - cameraAreaTop)

let targetAspect: CGFloat = 1.414 // height / width, A4 portrait
let maxWidth = min(availableWidth * 0.90, 430)
let maxHeight = cameraAreaHeight * 0.94
let widthByHeight = maxHeight / targetAspect

let viewfinderWidth = min(maxWidth, widthByHeight)
let viewfinderHeight = min(maxHeight, viewfinderWidth * targetAspect)
let viewfinderCenterY = cameraAreaTop + cameraAreaHeight * 0.50
```

约束要求：

| 设备 | 要求 |
| --- | --- |
| 小屏 | 优先保证底部横向预览列表和完成按钮完整展示，取景框可适当压缩 |
| 标准屏 | 取景框接近 A4 竖版比例，报告四边完整入框 |
| 大屏 | 限制最大宽度，避免取景框过宽导致用户离纸过远 |
| 横屏或异常比例 | 保持可用，不做崩溃；可降级为最大可用矩形 |

### 6.3 底部面板改进

MedicineBox 的底部面板内部控件随 `panelHeight` 线性缩放，检查报告相机建议改为固定合理区间：

1. 底部面板高度限定在 `196pt...252pt`。
2. 缩略图高度限定在 `72pt...92pt`。
3. 拍摄按钮大小限定在 `72pt...84pt`。
4. 完成按钮保持最小点击区域 `44pt`。
5. 文案使用 `lineLimit(2)` 和 `minimumScaleFactor(0.8)`，避免小屏溢出。

## 7. 相机页面交互

### 7.1 首次引导

首次进入展示 `ExaminationReportCameraGuideView`。

引导内容建议：

1. 将报告放在平整桌面上。
2. 保持纸张四边完整入框。
3. 避免反光、阴影和手指遮挡。
4. 拍清楚姓名、检查项目、时间、医院、所见和结论。
5. 多页报告可按顺序连续拍摄，检查报告最多 3 张，体检报告最多 6 张。

存储 key：

```text
spark.examination_report.camera.guide.has_seen
```

### 7.2 顶部栏

1. 左侧返回按钮：关闭相机，不保存未返回的内容。
2. 中间标题：`检查报告`。
3. 右侧帮助按钮：再次打开引导页。
4. 第一版可不展示前后摄像头切换；默认后置摄像头。

### 7.3 取景区

1. 展示报告纸张比例取景框。
2. 取景框外使用半透明遮罩。
3. 取景框下方展示拍摄提示，例如“保持报告四边完整入框，文字清晰无遮挡”。
4. 提示文案不依赖固定槽位，可根据已拍数量提示“第 N 张”。

### 7.4 底部面板

底部包含：

1. 横向滚动缩略图预览列表。
2. 中央拍摄按钮。
3. 完成按钮。

交互规则：

1. 用户拍摄并在确认页确认后，将图片追加到底部横向预览列表末尾。
2. 点击已有图片打开 QuickLook 预览。
3. 已有图片右上角展示删除按钮。
4. 删除图片后从底部预览列表移除，并重新计算序号。
5. 预览删除规则与用药相机保持一致：删除只影响当前已拍列表，不关闭相机，不影响其他图片。
6. 未达到最大数量时，拍摄按钮保持可用；达到上限后禁用或提示已达到上限。

## 8. 上传列表接入

### 8.1 相机策略扩展

当前：

```swift
enum MedicalAttachmentUploadCameraCover: Identifiable {
    case medicineBoxCustomCamera
    case systemCamera
}
```

建议新增：

```swift
case examinationReportCustomCamera
```

`id` 增加：

```swift
case .examinationReportCustomCamera:
    return "examinationReportCustomCamera"
```

`MedicalAttachmentUploadDocumentType.cameraCover` 调整：

```swift
case .medicineBox:
    return .medicineBoxCustomCamera
case .examinationReport, .healthExamReport:
    return .examinationReportCustomCamera
case .medicationPlan, .caseDocument:
    return .systemCamera
```

### 8.2 fullScreenCover 接入

在 `MedicalAttachmentUploadListSheet` 的 `fullScreenCover` 中新增分支：

```swift
case .examinationReportCustomCamera:
    ExaminationReportCameraSceneView(
        onCancel: { presentedCameraCover = nil },
        onImagesCaptured: { images in
            presentedCameraCover = nil
            for captured in images {
                if let file = saveUIImageToTemp(
                    image: captured.image,
                    namePrefix: "\(fileNamePrefix)_camera_page_\(captured.index)"
                ) {
                    localFiles.append(file)
                }
            }
        },
        maxCaptureCount: documentType.reportCameraMaxCaptureCount
    )
```

### 8.3 文件数量限制

1. 检查报告自定义相机最多返回 3 张图片。
2. 体检报告自定义相机最多返回 6 张图片。
3. 打开相机前仍使用 `ensureCanAddMoreFiles()` 校验。
4. 返回多图时仍需调用 `appendFiles` 或等价逻辑，避免超过剩余文件数。
5. 如果上传列表剩余可追加数量少于相机返回图片数，应按拍摄顺序追加到上限，并提示已达到最大文件数。
6. `MedicalAttachmentUploadDocumentType.maxFileCount` 需要同步支持体检报告拍摄 6 张；如业务仍要求体检报告最终只上传 1 个 PDF 文件，应在本工单中明确放宽图片上传上限。

## 9. 涉及文件

| 文件/模块 | 改动内容 |
| --- | --- |
| `SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/ExaminationReport/ExaminationReportCameraSceneView.swift` | 新增医疗报告自定义相机场景，支持检查报告 3 张、体检报告 6 张 |
| `SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/ExaminationReport/ExaminationReportCameraGuideView.swift` | 新增医疗报告拍摄引导页 |
| `SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/ExaminationReport/ExaminationReportCameraGuideStore.swift` | 新增引导展示状态存储 |
| `SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/ExaminationReport/ExaminationReportCapturedImage.swift` | 新增已拍图片模型，记录拍摄顺序 |
| `SparkClient/SparkClient/Projects/Features/CustomCamera/Presentation/Scene/ExaminationReport/ExaminationReportCameraViewfinderShape.swift` | 新增或复用改造报告取景框形状 |
| `SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/Shared/MedicalAttachmentUploadListSheet.swift` | 新增 `examinationReportCustomCamera` 相机策略、最大拍摄数量配置和 fullScreenCover 分支 |
| `SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/ExaminationReports/ExaminationReportsListPage.swift` | 无需大改，继续通过 `MedicalAttachmentUploadListSheet(documentType: .examinationReport)` 接入 |
| `SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/HealthExamReports/HealthExamReportsListPage.swift` | 无需大改，继续通过 `MedicalAttachmentUploadListSheet(documentType: .healthExamReport)` 接入 |
| `SparkClient/SparkClient/Projects/App/Resources/zh-Hans.lproj/Localizable.strings` | 增加检查报告相机相关中文文案 |
| `SparkClient/SparkClient/Projects/App/Resources/en.lproj/Localizable.strings` | 增加检查报告相机相关英文文案 |

## 10. 验收标准

### 10.1 入口验收

1. 在检查报告列表页点击底部“拍摄添加报告”打开上传列表。
2. 检查报告上传列表点击“拍照”时，打开 `ExaminationReportCameraSceneView`，本次最多拍摄 3 张。
3. 体检报告上传列表点击“拍照”时，打开 `ExaminationReportCameraSceneView`，本次最多拍摄 6 张。
4. 检查报告、体检报告拍照不再打开系统相机。
5. 药箱入口仍打开 `MedicineBoxCameraSceneView`。
6. 病例、服药计划入口仍按原策略打开系统相机。

### 10.2 相机交互验收

1. 首次进入检查报告相机展示拍摄引导页。
2. 再次进入默认不展示引导，但可点击帮助按钮重新查看。
3. 取景框为竖版 A4 友好比例，同时能容纳横版报告；小屏、大屏不遮挡底部面板。
4. 底部展示横向滚动预览列表，不展示固定槽位。
5. 拍摄确认后图片追加到预览列表末尾。
6. 已拍图片可以删除。
7. 点击已拍图片时打开 QuickLook 预览。
8. 不设置必拍项。
9. 未拍任何图片时完成按钮置灰或提示“请先拍摄报告图片”。
10. 已拍至少 1 张后允许完成并关闭相机。
11. 达到当前入口最大拍摄数量后，拍摄按钮禁用或提示已达到上限。

### 10.3 识别流程验收

1. 相机返回的图片被保存为临时 jpg。
2. 文件名前缀包含 `examination_report_camera_` 或 `health_exam_report_camera_`，并带拍摄顺序后缀。
3. 图片追加到上传列表后可预览、删除。
4. 点击上传列表确认后，继续调用 `startExaminationReportRecognition(files:)`。
5. 原有检查报告、体检报告识别、结果展示、保存入库流程不受影响。

### 10.4 回归验收

1. `MedicalAttachmentUploadListSheet` 的相册选择能力正常。
2. `MedicalAttachmentUploadListSheet` 的文件导入能力正常。
3. 最大文件数量限制仍生效。
4. 药箱多图拍摄不受检查报告相机新增分支影响。
5. 病例、服药计划等非报告上传入口不出现医疗报告相机页面。

## 11. 风险与注意事项

1. 医疗报告种类差异较大，单一 A4 竖版比例不能覆盖所有报告。第一版以常见纸质报告为主，横版影像报告先保证可拍，不做专门横版模式。
2. 多页报告第一版不做固定槽位，检查报告最多 3 张，体检报告最多 6 张；超过上限仍可通过相册或文件上传补充。
3. 自定义相机返回多图时需要遵守上传列表最大数量限制，避免绕过 `maxFileCount`。
4. 取景框只是拍摄指导，不等于真实裁切结果；第一版不承诺自动裁切和透视矫正。
5. 需求和代码必须统一“无必拍项”规则，避免出现 MedicineBox 当前文档与实现不一致的情况。

## 12. 待确认

| 项 | 待确认问题 | 建议 |
| --- | --- | --- |
| 槽位数量 | 是否固定槽位 | 已确认取消固定槽位，改为底部横向滚动预览 |
| 必拍项 | 是否设置必拍项 | 已确认取消必拍项，未拍图片时不允许完成即可 |
| 取景比例 | 是否需要横版/竖版切换 | 第一版不做切换，使用竖版 A4 友好比例并容纳横版报告 |
| 自动裁切 | 是否做纸张边缘检测和透视矫正 | 第一版不做，后续单独立项 |
| 适用范围 | 体检报告是否也复用此相机 | 已确认复用；检查报告最多 3 张，体检报告最多 6 张 |

---

## 工单 `EXAMINATION-REPORT-000002`：医疗检查报告列表页 UI 结构还原

### 工单状态

需求记录中。

## 1. 背景

本工单用于把医疗检查报告列表页的截图表现，用 plain text Markdown 记录为可验收的 UI 需求。

参考源码位置：

```text
SparkClient/SparkClient/Projects/Features/Home/Presentation/MedicalLists/ExaminationReports/ExaminationReportsListPage.swift:94-121
```

该区域定义了页面主体结构：

1. 页面使用 `ScrollView` 承载检查报告列表内容。
2. 页面背景为 `systemGroupedBackground`。
3. 导航标题为 `home.medical.list.examination_reports.title`，截图展示为“医疗检查报告”。
4. 顶部使用 `safeAreaInset(edge: .top)` 放置检查报告分类筛选条。
5. 页面启用系统搜索框，搜索提示复用 `home.medical.family_cabinet.search_prompt`，截图展示为“搜索药品名称、品牌、规格”。
6. 底部使用 `safeAreaInset(edge: .bottom)` 放置统一列表操作栏，成员 ID 有效时展示。

## 2. 页面整体 plain text UI

### 2.1 顶部与列表整体

```text
┌────────────────────────────────────────┐
│ 状态栏                                  │
│ 18:16                         信号 WiFi 电量 │
├────────────────────────────────────────┤
│  ◀                         医疗检查报告  │
├────────────────────────────────────────┤
│  🔍  搜索药品名称、品牌、规格             │
├────────────────────────────────────────┤
│                                        │
│  🧪  实验室检查                         12 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  📷  影像学检查                          6 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 影像学检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
├────────────────────────────────────────┤
│  + 手动添加        📷 拍摄添加报告        │
└────────────────────────────────────────┘
```

说明：

1. 返回按钮位于左上角，视觉为圆形浅色背景内的左箭头。
2. 标题居中显示，文本为“医疗检查报告”。
3. 搜索框位于标题下方，圆角胶囊样式，左侧搜索图标。
4. 页面主体按检查报告分类分组展示。
5. 底部操作栏固定在安全区底部，不随列表滚动。
6. 底部操作栏包含“手动添加”和“拍摄添加报告”两个入口。

### 2.2 分类分组标题

```text
🧪  实验室检查                                      12
```

```text
📷  影像学检查                                       6
```

字段规则：

1. 左侧图标取自 `ExaminationReportCategory.icon`。
2. 标题取自 `ExaminationReportCategory.titleKey`。
3. 右侧数字为当前分组报告数量。
4. 分类色彩：

| 分类 | 图标语义 | 主色 |
| --- | --- | --- |
| 实验室检查 | 试管 | 蓝色 |
| 影像学检查 | 相机 | 靛紫色 |
| 病理检查 | 文档放大镜 | 橙色 |

## 3. 实验室检查卡片 plain text UI

### 3.1 折叠重点信息状态

```text
┌────────────────────────────────────────┐
│ 🧪  肝功能 + 血清尿酸 + 血糖             查看详情 › │
│     (静脉)+ 血脂四项                             │
│     laboratory                         📎 3       │
│     2026/06/30   苏州工业园区星塘医院              │
├────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │    22    │  │    22    │  │    0     │ │
│  │   检查项  │  │   正常   │  │   重点   │ │
│  └──────────┘  └──────────┘  └──────────┘ │
├────────────────────────────────────────┤
│  ⚠  重点信息                              1  › │
├────────────────────────────────────────┤
│  轻点标题展开重点信息                         │
└────────────────────────────────────────┘
```

### 3.2 第二张实验室检查卡片示例

```text
┌────────────────────────────────────────┐
│ 🧪  乙肝两对半定量检测                  查看详情 › │
│     laboratory                         📎 1       │
│     2026/06/30   苏州工业园区星塘医院              │
├────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │    5     │  │    5     │  │    0     │ │
│  │   检查项  │  │   正常   │  │   重点   │ │
│  └──────────┘  └──────────┘  └──────────┘ │
├────────────────────────────────────────┤
│  ⚠  重点信息                              1  › │
├────────────────────────────────────────┤
│  轻点标题展开重点信息                         │
└────────────────────────────────────────┘
```

实验室检查卡片字段规则：

1. 左上角为分类圆形图标，实验室检查使用试管图标。
2. 主标题展示 `item.itemName`，允许两行换行。
3. 副标题展示 `category` 与 `subCategory` 组合；截图中展示为 `laboratory`。
4. 元信息行展示报告日期与医院名称。
5. 右上角“查看详情”在有明细时可点击进入详情页。
6. 附件入口展示回形针图标、附件数量和“附件”标签。
7. 统计区三列展示：

| 统计项 | 含义 | 颜色 |
| --- | --- | --- |
| 检查项 | `detailItems.count` | 蓝色 |
| 正常 | `detailItems.count - abnormalDetailItems.count` | 绿色 |
| 重点 | `abnormalDetailItems.count` | 红色 |

8. 当存在异常明细或报告结论文本时，卡片底部展示“重点信息”可展开区域。
9. 折叠状态显示“轻点标题展开重点信息”。

## 4. 影像学检查卡片 plain text UI

### 4.1 单行结论卡片

```text
┌────────────────────────────────────────┐
│ 📷  彩超检查报告单                      查看详情 › │
│     imaging                            📎 1       │
│     2026/06/30   苏州工业园区星塘医院              │
├────────────────────────────────────────┤
│     医生   缪小莉                              │
│     检查结论                                  │
│     双侧叶甲状腺无回声，考虑胶质囊肿，TI-RADS       │
│     分类 2 类                                  │
└────────────────────────────────────────┘
```

### 4.2 多行结论卡片

```text
┌────────────────────────────────────────┐
│ 📷  彩超检查报告单                      查看详情 › │
│     imaging                            📎 1       │
│     2026/06/30   苏州工业园区星塘医院              │
├────────────────────────────────────────┤
│     医生   缪小莉                              │
│     检查结论                                  │
│     肝内可见数个点状强回声，后方伴声影，考虑肝内钙化灶 │
│     未见明显异常                              │
│     未见明显异常                              │
│     未见明显异常                              │
└────────────────────────────────────────┘
```

### 4.3 影像报告列表继续滚动

```text
┌────────────────────────────────────────┐
│ 📷  胃镜诊断报告单                      查看详情 › │
│     imaging                            📎 1       │
│     2026/02/10   苏州大学附属第四医院 苏...        │
├────────────────────────────────────────┤
│     后续内容随卡片高度展示或滚动进入视口             │
└────────────────────────────────────────┘
```

影像学检查卡片字段规则：

1. 左上角为分类圆形图标，影像学检查使用相机图标。
2. 主标题展示 `item.itemName`。
3. 副标题展示 `imaging`。
4. 元信息行展示报告日期与医院名称，医院过长时单行截断。
5. 内容区优先展示医生与检查结论。
6. 检查结论对应 `item.impression`。
7. 影像学卡片不展示实验室检查的三列统计卡。

## 5. 底部操作栏 plain text UI

```text
┌────────────────────────────────────────┐
│  +  手动添加        📷  拍摄添加报告       │
└────────────────────────────────────────┘
```

交互规则：

1. 当 `memberID > 0` 时展示底部操作栏。
2. “手动添加”点击后打开 `ExamReportFormView` 新建检查报告。
3. “拍摄添加报告”点击后打开医疗附件上传 Sheet。
4. 上传确认后调用 `startExaminationReportRecognition(files:)`。
5. 底部操作栏使用毛玻璃背景，并与列表内容分隔。

## 6. 搜索与筛选

搜索范围：

```text
itemName
category
subCategory
organizationName
departmentName
doctorName
findings
impression
```

筛选规则：

1. 搜索关键字去除首尾空白后参与匹配。
2. 空搜索时展示当前分类下全部报告。
3. 匹配使用本地化标准包含匹配。
4. 分类筛选由 `ExaminationReportFilterBar` 控制，选中分类后只展示该分类报告。
5. 当前截图中重点表达的是系统搜索框、分组列表、卡片和底部操作栏；如顶部分类筛选条启用，应位于搜索区域下方或顶部安全区 inset 内。

## 7. 空状态 plain text UI

当 `filteredReports.isEmpty` 时展示空状态：

```text
┌────────────────────────────────────────┐
│                                        │
│                  🧪                    │
│             暂无检查报告                │
│        可通过手动添加或拍摄添加报告       │
│                                        │
└────────────────────────────────────────┘
```

要求：

1. 空状态最小高度约 320pt。
2. 空状态在页面主体区域居中展示。
3. 底部操作栏仍按 `memberID > 0` 规则展示。

## 8. 验收标准

1. 页面标题显示为“医疗检查报告”，并使用 inline 导航标题。
2. 页面顶部可搜索，搜索框提示文案与截图一致或按产品最终文案配置。
3. 列表按“实验室检查”“影像学检查”“病理检查”分组展示。
4. 分组标题左侧有分类图标，右侧显示当前分组数量。
5. 实验室检查卡片展示标题、副标题、日期、医院、附件数、三列统计和重点信息区域。
6. 影像学检查卡片展示标题、副标题、日期、医院、附件数、医生和检查结论。
7. 点击有明细的“查看详情”可以进入对应详情页。
8. 点击附件图标可以展开或收起附件列表。
9. 点击“重点信息”可以展开或收起重点内容。
10. 底部“手动添加”可以进入手动新建检查报告流程。
11. 底部“拍摄添加报告”可以进入上传 Sheet 并在确认后发起报告识别。
12. 列表滚动时底部操作栏保持固定，内容不被底部操作栏永久遮挡。

---

## 工单 `EXAMINATION-REPORT-000003`：检查报告列表时间优先排序与连续分类标题合并

### 工单状态

需求记录中。

## 1. 背景

当前检查报告列表需求和现有实现倾向于按大类顺序展示：

```text
实验室检查 -> 影像学检查 -> 病理检查
```

新的列表浏览目标是优先还原用户真实就医时间线。检查报告需要先按报告日期排序，再在时间流中按连续分类段落展示大类标题。

本工单覆盖 `EXAMINATION-REPORT-000002` 中“列表按大类分组展示”的旧规则：新规则为时间优先，大类标题只用于标识当前连续卡片段落。

## 2. 核心需求

1. 列表内容先按报告时间排序，不再先按大类整体分组。
2. 排序时间优先使用 `reportedAt`，缺失时使用 `performedAt`。
3. 默认按时间倒序展示，最新报告在前。
4. 当时间相同或连续日期内出现多条报告时，仍遵循整体时间顺序。
5. 在排序后的线性列表中，只有当当前卡片分类与上一张可见卡片分类不同时，才展示分类标题。
6. 连续出现的同一大类卡片，只在第一张卡片前展示一次标题。
7. 分类标题右侧数量展示当前连续段落内该分类卡片数量，不展示全列表该分类总数。
8. 分类标题包括：

| 分类 | 标题 | 图标 |
| --- | --- | --- |
| 实验室检查 | 实验室检查 | 试管图标 |
| 影像学检查 | 影像学检查 | 相机图标 |
| 病理检查 | 病理检查 | 文档放大镜图标 |

## 3. 排序与分段规则

### 3.1 排序规则

排序键：

```text
sortDate = reportedAt ?? performedAt
```

排序优先级：

```text
1. sortDate 倒序，日期越新越靠前
2. sortDate 相同时，保持接口返回的相对顺序
3. sortDate 缺失的报告放在列表末尾
```

### 3.2 连续分类段落规则

对排序后的报告列表，从上到下扫描：

```text
如果当前报告分类 != 上一个报告分类：
    开始一个新的分类段落
    展示分类标题
否则：
    继续追加到当前分类段落
    不重复展示分类标题
```

段落数量示例：

```text
排序后卡片分类序列：
实验室, 实验室, 实验室, 影像, 实验室, 实验室, 影像, 实验室, 实验室, 病理, 病理

分段结果：
实验室检查 3
影像学检查 1
实验室检查 2
影像学检查 1
实验室检查 2
病理检查 2
```

## 4. plain text UI 示例

```text
┌────────────────────────────────────────┐
│  🧪  实验室检查                         3 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  📷  影像学检查                          1 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 影像学检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  🧪  实验室检查                         2 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  📷  影像学检查                          1 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 影像学检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  🧪  实验室检查                         2 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 实验室检查报告卡片                 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  🔎  病理检查                           2 │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 病理检查报告卡片                   │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 病理检查报告卡片                   │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

说明：

1. 以上示例不表示大类全局分组，而表示时间排序后的连续分类段落。
2. “实验室检查”可以在列表中多次出现，但只有在它与上一段分类不同时才再次出现。
3. 如果连续 3 张都是实验室检查，只展示一次“实验室检查 3”标题。
4. 如果实验室检查中间被影像学检查打断，后续再次出现实验室检查时需要重新展示标题。
5. 病理检查标题使用病理分类图标；如最终视觉仍使用 `doc.text.magnifyingglass`，plain text 可用放大镜/文档语义表示。

## 5. 与筛选和搜索的关系

1. 搜索后先得到 `filteredReports`，再对过滤结果执行时间排序和连续分类分段。
2. 分类筛选选中“实验室检查”时，列表只剩实验室检查卡片，此时顶部只出现一个或多个实验室检查连续段。
3. 如果分类筛选后所有可见卡片都是同一分类，且中间没有其他分类打断，则只展示一次分类标题。
4. 如果后续保留顶部分类筛选条，筛选条只改变可见数据，不改变“时间优先排序”的主规则。

## 6. 实现建议

建议把列表展示数据从：

```text
[ExaminationReportCategory: [Report]]
```

调整为：

```text
[ExaminationReportTimelineSection]
```

建议模型：

```swift
struct ExaminationReportTimelineSection: Identifiable {
    let id: String
    let category: ExaminationReportCategory
    let reports: [SparkMedicalSyncAPI.RemoteExaminationReportWithAttachments]
}
```

生成流程：

```text
1. 获取 filteredReports
2. 按 reportedAt ?? performedAt 倒序排序
3. 遍历排序结果并计算每条报告的 ExaminationReportCategory
4. 当前分类与上一段分类相同时追加到上一段
5. 当前分类与上一段分类不同时创建新段
6. UI 按段落渲染标题和卡片
```

## 7. 验收标准

1. 列表不再固定展示“全部实验室检查 -> 全部影像学检查 -> 全部病理检查”的大类顺序。
2. 列表优先按报告时间倒序展示。
3. 同一连续分类段落只显示一次大类标题。
4. 大类标题右侧数量等于当前连续段落的卡片数。
5. 当同一分类被其他分类打断后，再次出现时需要重新展示该分类标题。
6. 搜索结果仍按时间优先排序，并按连续分类段落展示标题。
7. 空状态逻辑不变。
8. 实验室、影像学、病理三类卡片原有内容展示和点击交互不变。
